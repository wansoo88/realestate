# CLAUDE.md — 부동산 AI 자문 시스템 (pjt13-realestate)

> 이 파일은 data-product-studio 스킬셋이 인터뷰(1단계) 기반으로 자동 생성했습니다.
> Claude Code가 매 세션 자동으로 읽습니다. 프로젝트의 단일 진실 소스로 유지하세요.
> 도입 방식: **신규 프로젝트** — 인터뷰 기반으로 골격을 생성했습니다.

> ⚠️ 인터뷰에서 확정되지 않아 가정한 값: project_name (디렉터리명 기반 임시 — 원하는 이름 있으면 알려주세요), data_scale (수도권 아파트 실거래 10년치+매물 기준 수백만 건으로 추정), team (개인 프로젝트로 solo 가정), doc_store (기본값 confluence — 로컬 md만 쓸 거면 변경), runtime_agents 8종 구성 (제가 도메인 기준으로 제안한 로스터 — 2단계에서 조정 가능) — 확정 시 갱신하세요.

## 운영 모델 — PM/PMO 오케스트레이션
당신(어시스턴트)은 이 프로젝트의 **PM/PMO 오케스트레이터**입니다. herdr 워크스페이스에서 **사람이 질의하는 유일한 상대**이며, 전문 역할(subagent)들을 지휘합니다. **사람은 subagent와 직접 대화하지 않습니다** — 당신이 위임하고 종합해 보고합니다.
- 사람 요청 → 당신이 **계획** → subagent에 **이름으로 위임**(자동위임 불안정) → 결과를 **한국어로 요약 보고** → 결정은 **AskUserQuestion**.
- 상세 행동 규범은 **`orchestrator` 스킬**을 따르세요(Intake→Plan→Delegate→Track→Gate→Report).
- 진행 상태는 이 파일과 `skill.md`에 항상 최신으로 유지.

## 프로젝트 개요
- **무엇을**: AI 자문 웹앱 — 지도 기반 매물 추천 + 근거 리포트 (분석·예측 엔진 내장)
- **누가 씀**: 나 개인 (내 자산·소득·대출 한도 기준 실거주/투자 판단). 추후 가족·지인 확장 여지
- **핵심 문제**: ① 실거래가·매물·학군·교통·세금·정책이 흩어져 종합 판단이 안 됨 ② 내 자산/대출 한도에 실제로 맞는 후보를 못 고름 ③ 언제 사야 하는지 타이밍 근거가 없음 ④ 같은 단지도 동·층·타입별 가치 차이를 몰라 비싸게 사거나 안 팔리는 물건을 잡음
- **성공 기준**: ① 추천 근거가 납득돼 실제 매수 결정에 사용 ② 지역·매물 조사 시간 며칠→수분 ③ 내 조건(예산·세금·대출·선호)에 맞는 후보만 수천 건에서 5~10건으로 정확히 좁혀짐 ④ (장기) 추천 매물의 사후 수익률이 시장 평균 초과

## 기술 스택
| 항목 | 선택 |
|---|---|
| 클라우드 | 자체 서버 (iwinv VPS, `<DEPLOY_HOST>`) — Docker 기반 배포 ※ 실제 IP·계정은 `deploy-target.local.md`(git 제외) |
| DB | PostgreSQL + PostGIS (공간쿼리) |
| 프론트엔드 | React 반응형 웹 (모바일 퍼스트) → React Native 앱 확장 |
| 백엔드 | Python/FastAPI (Docker) |
| 데이터 소스 | 공공 오픈API (국토교통부 실거래가, 공공데이터포털, 부동산원), 포털 매물(호가) 수집 — 네이버 부동산 등, 공공 기초자료 (공시가격, 건축물대장, 학교알리미, 지하철/노선, 인구·세대), 정책·고시 문서 (국토부 보도자료, 지자체 정비사업 고시) |
| 데이터 규모 | 중규모(수십만~수백만) / 갱신 매물 준실시간(수시간) / 실거래가 일 1회 배치 / 정책·인프라 주 1회 |

## 서비스 범위 (도메인)
| 항목 | 확정 내용 |
|---|---|
| 대상 지역 | 수도권 전체 (서울 25개구 + 경기 + 인천) |
| 대상 매물 | **아파트** (재건축 포함). 오피스텔·빌라·상가는 범위 밖(2차 확장 후보) |
| 지도 | **카카오맵 API** — 주소→좌표 변환, 마커 군집(클러스터링) |
| 모바일 | **모바일 퍼스트 반응형 웹 → React Native 앱** (웹 선행, 앱은 검증 후 확장. 컴포넌트 로직 재사용 전제) |
| AI 에이전트 위치 | **런타임 + 개발 보조 양쪽** — 웹앱이 Claude API로 전문가 에이전트를 호출해 분석·근거 생성하고, 동일 도메인 지식을 Claude Code 스킬로도 보유 |

### ⚠️ 도메인 리스크 (설계 시 반드시 반영)
- **호가 데이터**: 공공 오픈API에는 "현재 나와있는 매물(호가)"이 없고 과거 체결가만 있음. 포털 수집은 이용약관 이슈가 있으므로 **개인 비상업 용도 전제 + robots/약관 준수 + rate limit**, 그리고 **공공API만으로도 서비스가 성립하도록 이중화**한다.
- **실거래가 지연**: 계약일 기준 신고에 최대 30일 지연 → "현재 시세"는 항상 추정치임을 UI에 명시.
- **서버 노출**: 공인 IP VPS이므로 `root` 직접 SSH 금지, 배포 전용 계정 + 키 기반 접속 + 방화벽.
- **투자 자문 성격**: 개인 판단 보조 도구이며 투자 권유가 아님을 리포트에 고지.

## 런타임 전문가 에이전트 (웹앱 안에서 분석 수행)
> 사용자가 자산·선호·기피 조건을 입력하면 아래 에이전트가 분석하고, `portfolio-advisor`가 **"언제 · 어느 단지 · 어느 동 · 왜"** 를 사유와 함께 종합한다.

| ID | 역할 | 담당 |
|---|---|---|
| `listing-researcher` | 매물 리서처 | 조건에 맞는 매물 수집·정규화·중복 제거, 허위/미끼매물 탐지, 급매·장기 미거래 물건 식별 |
| `location-analyst` | 지역 전문가 | 학군(학구도·학업성취도·학원가), 교통(역세권·GTX·신설노선 호재), 생활 인프라, 유해시설·소음 |
| `finance-tax-advisor` | 세금·대출 전문가 | 취득세·보유세(재산세/종부세)·양도세 시뮬레이션, LTV/DSR/DTI 한도, **내 자산 기준 실구매 가능 금액** |
| `valuation-trader` | 매매 전문가 | 단지/**동**/층/향/타입별 가격 편차, 적정가 밴드, 호가–실거래 갭, 거래회전율·환금성 |
| `policy-researcher` | 정책 연구가 | 규제지역·대출규제·세제개편·공급계획·정비사업 단계 추적 및 대상 매물 영향 예측 |
| `market-timing-analyst` | 타이밍 분석가 | 전세가율·미분양·매수우위지수·금리·입주물량 기반 **매수 타이밍 신호** |
| `risk-auditor` | 리스크 검증가 | 권리관계·근저당, 재건축 사업성·추가분담금, 깡통전세, 노후도·하자 등 하방 리스크 |
| `portfolio-advisor` | 종합 자문가 | 위 의견을 사용자 자산·선호·**기피 조건**으로 가중 종합 → 최종 순위 + 사유 리포트 |

> MVP는 `listing-researcher` · `finance-tax-advisor` · `valuation-trader` · `location-analyst` · `portfolio-advisor` 5종으로 시작하고, 나머지는 2차. (2단계 설계에서 확정)

## 위임 대상 역할(subagent) — PM/PMO가 지휘
| 단계 | 위임할 역할 |
|---|---|
| 인터뷰/기획 | service-strategist |
| 설계 | infra-architect · software-architect · db-architect · **ui-ux-designer**(신설) |
| 데이터 수집 | **data-engineer**(신설) — 공공API·크롤러 파이프라인, 정규화·중복제거, 배치 스케줄 |
| 구현 | software-architect → frontend-react / backend-docker / **mobile-react-native**(신설) |
| 리뷰(필수) | code-reviewer · security-reviewer |
| 테스트 | qa-engineer |
> 오케스트레이터가 위 역할을 **이름으로 명시 호출**해 위임(자동위임 불안정). 사람은 이들과 직접 대화하지 않음.
> **(신설)** 표시 역할은 아직 `.claude/agents/`에 없음 — 2단계 시작 시 생성한다.
> `ui-ux-designer`는 사용자 요청으로 추가된 역할: **모바일 퍼스트 UI/UX** — 지도 조작성, 한 손 도달 범위, 리스트↔지도 전환, 리포트 가독성, 디자인 시스템 담당.

## 6단계 파이프라인 현황
- [x] 1. 인터뷰 — 완료 (이 파일 생성)
- [x] 2. 설계 — 완료 (2026-07-24) · `docs/02-design/`
  - `architecture.drawio` + `architecture.md` — 단일 VPS + Docker Compose (AWS 3-tier 아님)
  - `erd.md` + `schema.dbml` — PostGIS 데이터 모델
  - `api-spec.md` — API 계약
  - `security.md` — 위협모델·암호화·서버 하드닝 (R-01~R-04 해소)
  - `agents/` — 런타임 전문가 에이전트 8종 명세 (MVP 5종)
  - `ux/` — 모바일 퍼스트 UI/UX
- [ ] 3. 구현 (**code-review·security-review 무조건**)
- [ ] 4. 테스트 (단위·e2e)
- [ ] 5. 모니터링
- [ ] 6. 최종점검(인수인계)

## 산출물 저장 원칙
- 문서 허브: **Confluence** (doc_store=confluence)
- 아키텍처: **draw.io**, ERD/UML: **Mermaid/PlantUML**, e2e: **Playwright** — 무료 우선.
- 외부(Confluence 등) 업로드 전 항상 사용자 확인.

## 제약 / 보안
- 사용자 규모: 소수(팀 내) · 일정: 2~3개월 · 팀: 나 혼자
- 민감정보: 개인 금융정보 (본인 자산·소득·대출한도) + 로그인 계정 — 암호화 저장 필요
- 보안 요구: 자산·소득·대출 정보 서버 저장 + 컬럼 암호화(예: pgcrypto/앱단 AES) + 로그인 인증 필수. 공인 IP 노출 서버이므로 root 직접 SSH 금지 → 배포 전용 계정 + 키 기반 접속 + 방화벽/포트 제한. HTTPS 필수. 포털 매물 수집은 개인 비상업 용도 전제 + robots/이용약관 준수 + 수집 속도 제한(rate limit), 공공API만으로도 서비스가 성립하도록 이중화.

## 진행 방식 — 가이드 모드
각 단계는 **끝나면 요약 + 확인 후 다음 단계로 이어간다**(멈춤 지점 유지):
1. 해당 단계 작업 수행(서브에이전트는 이름으로 명시 호출).
2. 산출물을 3~5줄 한국어로 요약.
3. AskUserQuestion으로 묻는다: **"다음 단계(N: 이름)로 갈까요?"** [예 / 이번 단계 수정 / 여기서 멈춤].
4. **예** → 다음 단계 자동 진행. **수정** → 이번 단계 보완. **멈춤** → skill.md 갱신 후 종료.
5. ⚠️ **3단계 리뷰 게이트는 하드 스톱**: code-review·security-review가 모두 passed 되기 전에는 다음으로 못 넘어간다(훅이 커밋도 차단).
> 사용자가 "쭉 진행"을 명시하면 소프트 확인은 줄이되, 3단계 리뷰 게이트는 항상 지킨다.

## 규칙
- 모든 사용자 대면 소통은 **한국어**, 짧고 명확하게.
- 진행은 `/next-stage`(가이드 모드로 다음 단계) 또는 해당 단계 스킬 호출.
- **스킬 스크립트 경로**: 플러그인 설치면 `${CLAUDE_PLUGIN_ROOT}/skills/<스킬>/scripts/...`, 전역(복사) 설치면 `~/.claude/skills/<스킬>/scripts/...` 를 절대경로로 실행. (`${CLAUDE_PLUGIN_ROOT}`가 안 잡히면 후자)

---
*생성일: 2026-07-24 · data-product-studio v0.1*
