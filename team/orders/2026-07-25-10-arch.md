---
id: 2026-07-25-10-arch
to: re-arch
rev: 1
gate: review-required
status: dispatched
---

## 목적 (왜 하는가)
re-data 가 세율을 채우며 **로더 구조 한계 3건**을 에스컬레이션했다. 지금 로더는 단일 요율만
받아서 6.27 대책의 **수도권 주담대 6억 절대한도**·스트레스 DSR·6~9억 누진산식을 표현 못 한다.
이대로면 실구매 가능액이 **과대 산정**된다(6억캡 미반영이 가장 큼). 로더가 이를 표현·로딩하도록
확장한다. 겸사겸사 마이그레이션 실검증에서 나온 자잘한 것 3건도 처리한다.

## 근거
- `config/tax_rules.yaml` L1(6~9억 누진)·L2(6억캡·스트레스DSR)·L3(특례)
- `backend/app/domain/rules/loader.py` (현재 단일 브래킷)
- `docs/03-build/code-review-log.md` CR-008 (needs_db 테스트 버그, requirements pytest 누락)
- re-arch 03-arch 보고 (003 스키마 공백: school_district 기준연도·도로선형)

## 작업
### A. 로더 스키마 확장 (핵심 — engine 계산은 re-domain 몫, 너는 **데이터 표현·로딩**까지)
- [ ] **수도권 6억 절대한도**: `lending` 에 `absolute_cap_krw`(지역조건부) 표현. 로더가 파싱해
      `RuleSet` 으로 노출. **계산 적용(min)은 re-domain 이 engine 에서** — 너는 값을 제공만.
- [ ] **스트레스 DSR**: 가산금리(stress_rate_pct) 를 lending 규칙에 표현·로딩.
- [ ] **6~9억 누진산식**: 현재 0.5억 서브밴드 근사(±0.2%p) 대신, 로더에 **누진 산식 밴드 타입**
      (`formula: progressive`, 계수)을 추가해 engine 이 연속 계산할 수 있게 데이터로 표현.
      기존 고정요율 브래킷과 **공존**해야 한다(하위호환).
- [ ] 확장 인터페이스를 `docs/domain/lending-rules-contract.md` 로 문서화 → re-domain/re-data 입력.

### B. 부수 (CR-008 후속)
- [ ] **needs_db 테스트 버그 수정**: `test_근거없는_agent_finding은_저장되지_않는다` 2번째 케이스에서
      SQLAlchemy `text()` 가 `'{"a":1}'` 의 `:1` 을 바인드 파라미터로 오해석. `exec_driver_sql`
      사용 또는 콜론 이스케이프. 목표 needs_db **29/29**.
- [ ] **requirements 분리**: `backend/requirements-dev.txt` 신설(pytest 등). 런타임과 분리.
- [ ] **003 마이그레이션**: `school_district` 에 기준연도 컬럼, `road_segment`(도로선형) 테이블 추가
      → `crosses_main_road` 판정 가능하게. LocationRepository 도 이에 맞춰 갱신.

## 산출물 (정확한 경로)
- `backend/app/domain/rules/loader.py` (스키마 확장, 하위호환)
- `backend/migrations/003_*.sql`
- `backend/requirements-dev.txt`
- `backend/tests/test_postgis_repo.py` (버그 수정)
- `docs/domain/lending-rules-contract.md`

## 완료 기준 (DoD — 검증 가능하게)
- 로더가 6억캡·스트레스DSR·누진밴드를 파싱해 RuleSet 으로 노출한다(단위 테스트).
- 기존 세율 로딩·계산 테스트가 회귀 없이 통과한다(하위호환).
- needs_db 가 29/29 (테스트 버그 수정 확인 — 로컬 DB 없으면 정적/논리 검증으로라도 근거 제시).
- 003 이 기존 스키마와 충돌 없이 추가된다.
- lending-rules-contract.md 로 re-domain(engine)·re-data(값)가 바로 착수 가능.

## 경계 (하지 말 것)
- **affordability engine 계산 로직을 직접 바꾸지 말 것** — 6억캡 min·스트레스DSR·누진 계산은
  re-domain 소유. 너는 **로더가 값을 표현·제공**하는 데까지.
- tax_rules.yaml 값 자체는 re-data 몫 — 예시/스켈레톤만.
- 실서버·실DB 파괴적 작업 금지.

## 보고
```powershell
python scripts/tell.py re-pm "DONE 2026-07-25-10-arch : 로더확장(6억캡·스트레스DSR·누진밴드) / needs_db 29 / 003 / contract 문서 / 이슈"
```
