---
id: 2026-07-25-04-data
to: re-data
rev: 1
gate: review-required
status: dispatched
---

## 목적 (왜 하는가)
① 자금 계산 API 가 지금 503 을 낸다 — `config/tax_rules.yaml` 이 비어 있기 때문이다.
   공식 출처에서 세율·대출한도를 확인해 채우면 F2 가 실제로 동작한다.
② 국토부 실거래가 수집을 실행 가능한 상태로 만들어 지도·시세 분석에 실데이터를 넣는다.

## 근거
- `docs/02-design/agents/02-finance-tax-advisor.md` (세율은 출처·기준일자 필수)
- `backend/app/domain/rules/loader.py` (로더가 요구하는 형식)
- `backend/app/ingest/molit.py` (수집기 — 파싱은 픽스처로 검증됨, 실호출 미검증)
- SR-004 SR4-3(robots 파서), CR-004 I-08(세율 실제값)

## 작업
### A. 세율 채우기 (우선)
- [ ] **취득세**: 지방세법 §11/§13의2 에서 주택수·가격·면적 구간별 세율 확인.
      본세 + 지방교육세 + 농특세를 각각 `rate_pct`/`extras.*_pct`(거래금액 대비 %)로.
- [ ] **중개보수**: 공인중개사법 시행규칙 별표에서 가격 구간별 상한요율·한도액(`cap_krw`).
- [ ] **LTV/DSR/DTI**: 현행 규제(지역·주택수·목적별). 규제지역/비규제 구분이 필요하면
      PM 경유로 `re-arch` 에 스키마 확장 요청(현재 로더는 단일 ltv/dsr/dti 만 받음).
- [ ] 모든 값에 `source`·`source_url`·`as_of` 를 실제 근거로 채우고 `status: verified` 로.
- [ ] **위택스·국세청 계산기와 대조**해 최소 3개 시나리오 일치 확인(취득세).
- [ ] 검증 케이스를 `backend/tests/test_tax_rules_real.py` 로 추가(실제 세율 회귀 방지).

### B. 수집 실행 준비
- [ ] 공공데이터포털 국토교통부 실거래가 서비스키 발급 절차 정리(사람이 발급해야 하면 안내).
- [ ] `config/sources.yaml` 작성 — 소스 우선순위·주기·rate limit(security.md §5).
- [ ] robots.txt 파서 골격(`backend/app/ingest/robots.py`) — 포털 수집 착수 전 필수(SR4-3).
- [ ] 수집 실행 루프(`backend/app/ingest/runner.py`) — rate limit·ingest_log 기록·증분 수집.

## 산출물 (정확한 경로)
- `config/tax_rules.yaml` (status: verified, 값 채움)
- `config/sources.yaml`
- `backend/app/ingest/robots.py`, `backend/app/ingest/runner.py`
- `backend/tests/test_tax_rules_real.py`

## 완료 기준 (DoD — 검증 가능하게)
- `load_rules("config/tax_rules.yaml")` 이 예외 없이 로딩된다(status: verified).
- 취득세 3개 시나리오가 위택스 계산 결과와 일치한다(출처 링크 제시).
- 자금 계산 API 가 503 대신 실제 금액을 반환한다.
- 수집 실행 시 `ingest_log` 에 성공/실패가 기록된다.

## 경계 (하지 말 것)
- **출처 없이 세율을 채우지 말 것.** 기억·추정 금지 — 반드시 공식 출처 링크(G2·G4).
- robots·rate limit·약관을 위반하는 수집 코드 금지. 애매하면 수집하지 말고 PM 에 에스컬레이션.
- 원천 데이터(raw)를 저장소에 커밋 금지(`.gitignore: data/raw/`).
- API 키를 코드/설정에 하드코딩 금지 — `.env` 에서만.

## 보고
```powershell
python scripts/tell.py re-pm "DONE 2026-07-25-04-data | 세율 채움(출처 N건) | 위택스 대조 결과 | 수집 준비 상태 | 이슈"
python scripts/tell.py re-pm "BLOCKED 2026-07-25-04-data | 서비스키 발급 필요 | 필요: 사람 발급"
```
