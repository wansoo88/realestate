---
id: 2026-07-25-05-review
to: re-review
rev: 1
gate: none
status: dispatched
---

## 목적 (왜 하는가)
3단계 코드(커밋 f46d597·030a0af·f7088e4)의 리뷰는 herdr 다운으로 **PM 이 자체 검토**했다.
CHARTER §2 역할 분리 원칙상 이건 정식 판정이 아니다. re-review 가 **독립 재감사**해
CR-004~006·SR-004~005 를 확정하거나 반려한다. 특히 G2(근거 감사)와 SR4-2(자산 유출)를 깨보라.

## 근거
- `docs/03-build/code-review-log.md` CR-004~006 (PM 자체 검토 표시)
- `docs/03-build/security-review-log.md` SR-004~005 (동일)
- `team/CHARTER.md` §4 게이트, `team/roles/re-review.md`

## 작업
- [ ] **G2 근거 감사** — `backend/app/agents/` 를 깨본다:
      · `evidence` 없는 finding 이 저장될 경로가 정말 없는가?
      · 추정(basis=estimated_from_location) 신뢰도 상한이 우회 가능한가?
      · LLM 폴백이 출처 없는 문장을 만들지 않는가?
- [ ] **SR4-2 재검증(최우선)** — `assert_no_secrets` 를 실제로 깨보라:
      · 금액을 억/만원/한글("삼억")로 표기하면 우회되는가?
      · `forbidden_amounts` 에 안 들어간 파생값(월상환액 등)이 새는가?
      · 프롬프트 외 경로(로그·에러·예외 메시지)로 금액이 나가는가?
- [ ] **G3 개인정보** — 자산 암호화·로그 마스킹을 깨본다. AAD 우회, nonce 재사용, 복호화 오라클.
- [ ] **IDOR** — `user_id` 조건 없는 사용자 자원 접근 경로를 전수 탐색.
- [ ] 자금 계산 로직 반례 탐색 — 이분탐색 경계, 음수·0·극단 입력, 세율 구간 경계.
- [ ] 판정 결과를 `.review-state.json` 과 리뷰 원장에 **re-review 명의로** 기록.
      PM 자체 검토 항목을 CONFIRMED / 반려로 갱신.

## 산출물 (정확한 경로)
- `docs/03-build/code-review-log.md` (CR-007: re-review 재감사)
- `docs/03-build/security-review-log.md` (SR-006: re-review 재감사)
- `docs/03-build/.review-state.json` (reviewer 를 PM→re-review 로 갱신)

## 완료 기준 (DoD — 검증 가능하게)
- SR4-2 를 최소 5가지 방법으로 우회 시도하고 각 결과를 기록.
- 발견한 결함마다 **무엇을 고치면 통과인지** 구체적으로 명시(FAIL 시).
- G2·G3·IDOR 각각에 CONFIRMED 또는 결함 목록.
- 재감사 결과가 원장에 re-review 명의로 남는다.

## 경계 (하지 말 것)
- **분석 로직·구현 코드를 직접 고치지 말 것**(이해상충). 고치는 건 만든 사람 몫.
- 게이트 기준을 완화하지 말 것 — 입력을 고치지 판정 기준을 고치지 않는다.
- "조건부 통과" 금지 — 조건이 있으면 FAIL.

## 보고
```powershell
python scripts/tell.py re-pm "PASS 2026-07-25-05-review | 재감사 완료 | SR4-2 우회 N회 전부 차단 | 잔여 위험"
python scripts/tell.py re-pm "FAIL 2026-07-25-05-review | 대상 | 핵심 결함 | 통과 조건"
```
