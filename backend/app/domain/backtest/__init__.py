"""자기 채점(백테스트) 도메인 — **순수 함수만**. DB·엔진·HTTP 를 모른다.

설계 정본: `docs/02-design/backtest.md`

무엇을 하는 패키지인가
----------------------
`CLAUDE.md` 성공기준 ④ = *"추천 매물의 사후 수익률이 시장 평균 초과"*. 그걸 잴 코드가
저장소에 **0건**이었다(`enhancement-research.md` §0). 이 패키지가 그 0을 1로 만든다.

과거 시점 `T` 로 시계를 되돌려 **그때 알 수 있었던 것만으로** 후보를 점수 매기고,
`T+12개월` 실현 수익률을 벤치마크와 비교한다.

⚠️ 이 패키지가 재지 **못하는** 것 (읽는 사람이 결과보다 먼저 알아야 한다)
------------------------------------------------------------------------
총점 6축 중 **학군(.20)·교통(.20)·생활인프라(.15)** 는 과거 상태를 알 방법이 없어
채점 대상에서 **제외**했다 — 가중치의 **55%** 다. 실제로 채점되는 것은 시변 축 셋
(가격매력 .20 · 환금성 .15 · 가격추세 .10, 합 .45)뿐이다. 근거와 대안 검토는
`docs/02-design/backtest.md` §2-F.

모듈 배치
---------
    models      면적대·셀·가격시점·폴드 등 데이터 형태
    asof     ★ as-of 뷰. **미래 정보 차단이 전부 여기 한 곳에 있다**
    outcome     실현 수익률·벤치마크·생존 편향 정책
    collect  ★ 스트리밍 집계. **행을 쌓지 않는 것이 여기 한 곳에 있다**(메모리 상한)
    scorers     점수 함수(전부 as-of 셀만 본다) + 무작위/상수 대조군
    engine      폴드 실행·무작위 대조군·국면 라벨·집계/판정
    repository  Protocol (도메인은 SQL 을 모른다)
"""
