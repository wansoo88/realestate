[pytest]
testpaths = tests
python_files = test_*.py
addopts = -q --strict-markers
markers =
    needs_db: 실제 PostGIS 가 필요한 테스트 (로컬 Docker 부재로 기본 제외)
filterwarnings =
    ignore::DeprecationWarning:argon2.*
