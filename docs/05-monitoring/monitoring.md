# monitoring.md — 운영 감시 (설치·임계값·근거)

> 대상 서버: iwinv VPS (`deploy-target.local.md` 참조) · 동거 서비스 3개(itsmine · autobtc/recostock · pjt12-adsense)와 한 호스트를 나눠 쓴다.
> 설치일: 2026-07-30 · 1차 수정: 2026-08-01(CR-040 / SR-036R). 이 문서의 숫자는 전부 그 서버에서 재 본 값이다.

---

## 0. 왜 만들었나 — 사고 4건이 전부 사람 눈에 우연히 걸렸다

| 사고 | 어떻게 드러났나 | 지금은 무엇이 잡는가 |
|---|---|---|
| 운영 DB OOM 재기동 | 작업 중 우연히 | `memory.events` 의 `oom_kill` 카운터 증가 → 즉시 |
| nginx 설정 오류로 메인 404 | 배포 후 확인 명령 | 메인/health 200 검사 + **번들 검사** (2회 연속) |
| 자산 파생값이 접근 로그에 평문(회전본 0644) | 보안리뷰가 로그를 뒤져서 | 실제 파일 모드 검사 + 로그 내 쿼리스트링 검사 |
| 시장지수 크론이 조용히 죽을 수 있는 상태 | 리뷰 지적 | `market-index.sh` 자기 단언 + `job-run.sh` 종료코드 알림 + DB 기준월 신선도 |

추가로 **설치 과정에서 새로 드러난 사실**이 두 개(§5), **감시 자신이 낸 결함 두 개**(§8)가 있다.

---

## 1. 무엇이 어디에 있는가

| 항목 | 경로 |
|---|---|
| 저장소 원본(진실 소스) | `deploy/monitor.sh` · `deploy/monitor-lib.sh` · `deploy/job-run.sh` · `deploy/market-index.sh` |
| 감시 자체의 회귀 검사 | `deploy/monitor-selftest.sh` (§9) |
| 서버 배치 경로 | `/opt/realestate/scripts/{monitor.sh,monitor-lib.sh,job-run.sh,market-index.sh}` |
| 상태 저장 | `/var/lib/realestate-monitor/` (0700 · kv/ alerts/ jobs/) |
| 감시 자체 로그 | `/var/log/realestate-monitor.log` (0640 · 1MiB 넘으면 스스로 3000줄로 절단) |
| 배치 로그 | `/var/log/realestate_market_index.log` (**0640** — `job-run.sh` 가 매 실행마다 맞춘다) |
| 알림 자격증명 | `/etc/realestate-monitor.env` → 없으면 `/root/pjt12-adsense/.env` (읽기만) |

> ⚠️ 서버 사본은 `git` 이 추적하지 않는다. 스크립트를 고쳤으면 **다시 올려야** 반영된다.
> 저장소만 고치면 서버에서 도는 것은 옛 코드다:
> ```bash
> scp deploy/monitor.sh deploy/monitor-lib.sh deploy/job-run.sh deploy/market-index.sh \
>     deploy/monitor-selftest.sh root@<HOST>:/opt/realestate/scripts/
> ssh root@<HOST> 'chmod 750 /opt/realestate/scripts/{monitor.sh,job-run.sh,market-index.sh};
>                  chmod 640 /opt/realestate/scripts/monitor-lib.sh;
>                  bash -n /opt/realestate/scripts/monitor.sh'
> # 올린 뒤 반드시 sha256 대조 (5/5 일치해야 한다)
> sha256sum deploy/*.sh ; ssh root@<HOST> 'sha256sum /opt/realestate/scripts/*.sh'
> ```

### 크론 (기존 6건은 그대로 두고 2건 추가 + 1건을 감싸기만 했다)

```cron
# 5분마다 — 빠른 검사
*/5 * * * * /opt/realestate/scripts/monitor.sh --fast >/dev/null 2>&1
# 매일 09:05 — 전체 검사 + 요약 1통
5 9 * * *  /opt/realestate/scripts/monitor.sh --daily >/dev/null 2>&1
# 시장지수 배치 — job-run.sh 로 감싸서 종료코드를 읽게 했다
# `umask 027` 은 크론 리다이렉트가 로그를 0644 로 만드는 것을 막는다(SR36-2).
10 4 1 * * umask 027 && /opt/realestate/scripts/job-run.sh market-index -- /opt/realestate/scripts/market-index.sh >> /var/log/realestate_market_index.log 2>&1
```

크론 백업: `/root/crontab.backup.20260730-190729`(최초 설치 직전) ·
`/root/crontab.backup.20260801-*`(umask 추가 직전). 되돌리려면 `crontab <백업파일>`.

> **배치가 매월 1일 04:10 에만 돈다는 사실은 감시의 전제**다(§3 #11).
> 이 주기를 바꾸면 `market_expected_ym()` 의 전제가 깨진다 —
> `monitor-selftest.sh` 의 T8 이 이 크론 줄과 스크립트를 대조해 붙잡는다.

---

## 2. 알림은 어디로 가나

**텔레그램** — 동거 서비스 `pjt12-adsense` 가 이미 쓰는 봇(`stacktutilversebot`)과
**같은 개인 채팅**으로 간다. 새 채널을 만들지 않았다.

- 근거: 그 채널이 실제로 사용자에게 닿는다는 것을 **전송해서 확인**했다(HTTP 200).
  서버에 MTA·mailx·msmtp 는 없고(설치 안 됨), recostock 의 텔레그램 토큰은
  서버가 아니라 GitHub Actions 쪽에 있다(`/etc/recostock.env` 에는 `GH_DISPATCH_TOKEN` 뿐).
  즉 **서버에서 사용자에게 닿는 유일한 기존 경로가 이것**이다.
- 토큰을 복사하지 않는다. 복사하면 그쪽이 토큰을 바꿀 때 **우리만 조용히 죽는다**.
  대신 파일에서 필요한 두 값만 뽑아 쓴다(`source` 하지 않는다 — 남의 `.env` 를
  실행하는 것은 코드 실행이다).
- 우리 전용 봇으로 갈라내고 싶으면 `/etc/realestate-monitor.env` 를 0600 으로 만들고
  `TELEGRAM_BOT_TOKEN` / `TELEGRAM_CHAT_ID` 두 줄만 넣으면 **그쪽이 우선**한다. 코드 수정 불필요.

### 알림 본문에서 지우는 것 — `scrub()` 이 실제로 하는 일

나가는 문자열은 전부 `scrub()` 를 통과한다. **규칙을 있는 그대로 적는다**(예전 판은
*"9자리 이상 숫자는 `<num>`"* 한 줄이었는데, 그 문장이 실제 동작보다 넓었다 — CR40-4):

| 대상 | 규칙 | 예 |
|---|---|---|
| DSN 비밀번호 | `스킴://사용자:비밀@` → `사용자:<redacted>@` | psycopg 트레이스백이 `DATABASE_URL` 을 통째로 뱉는다 |
| 봇 토큰 | `숫자{6,}:영숫자{20,}` → `<token>` | 형태가 눈에 안 띈다 |
| Bearer/JWT | `Bearer <16자 이상>` → `<redacted>` | |
| **이름 없이 값만 (SR39-4)** | `sk-ant-…` · `KakaoAK <키>` · `Basic <base64>` · 맨 JWT(`eyJ….….…`) → `<redacted>` | 규칙 ④는 `이름=값` 꼴만 본다. 그런데 실제 트레이스백·에러메시지는 **값만 던진다** (`invalid x-api-key sk-ant-…`). 접두가 고유해 오탐이 없다. ⚠️ 숫자 규칙보다 **앞**에 둔다 — 뒤에 두면 `KakaoAK 0123…` 의 숫자만 먼저 지워져 영문 절반이 남는다(실측) |
| KEY 꼴 | `*KEY`/`*TOKEN`/`*SECRET`/`*PASSWORD` 뒤의 `=` 또는 `:` **(사이의 따옴표 `"` `'` `` ` `` 와 뒤 공백 허용)** 다음 값 | `SERVICE_KEY=…` · `KAKAO_REST_KEY: …` · **`{"api_key": "…"}`** · **`{'TOKEN': '…'}`** |
| 금액 ① | **구분자 없는 9자리 이상** 숫자 → `<num>` | `1026560000원` (앞뒤에 한글이 붙어도 지운다) |
| 금액 ② | **쉼표 3자리 묶음이 2개 이상**(=1,000,000 이상) → `<num>` | `1,026,560,000원` — 앱이 금액을 쓰는 형식(`f"{v:,}원"`) 그 자체 |
| 금액 ③ | **금액 토큰이 붙은 수**는 자릿수 무관 → `<num>` | 뒤에 `원`/`만원`/`억`/`억원` 이 붙거나, 앞에 `cash`/`price`/`budget`/`amount`/`krw`/`salary` 계열 키가 `=`·`:` 로 붙은 수. 키와 구분자 사이의 따옴표는 `"` `'` `` ` `` **셋 다** 받는다(SR38-4) |

**금액 ③ 을 왜 넣었나 (CR41-4).** 예전 판은 ①②만 두고 *"금액은 이 시스템에서 항상
백만 이상이고 쉼표를 붙이는 것은 금액뿐이라 이 선이 성립한다"* 고 적었는데,
**그 문장이 사실이 아니었다** — 백만은 7자리라 ①(9자리)에 애초에 안 걸리고,
쉼표 1묶음 금액(`850,000원`)도 ②에 안 걸린다. 실제로 리뷰가 그냥 통과시켜 보인 형태:
`50000000원`(8자리)·`3500000원`(7자리)·`5,000만원`·**`102,656만원`(국토부가 쓰는 만원 단위)**·
`4.2억`·`{"cash_krw": 50000000}`·`X-Cash: 50000000`. **③ 을 넣은 뒤 이 7종이 전부 지워진다.**

**일부러 안 지우는 것 — 그리고 그 이유.**
자릿수를 8로 내려서 막지 **않은** 이유는 하나다. 우리가 알림에 실제로 찍는 운영 수치가
같이 죽는다: `auth.log` 크기·오프셋(`44049707` = 8자리)·바이트 수(`1048576`)·
실행 횟수(`288`)·소요 시간(`0.36초`)·기준월(`2026-06`). 특히 **auth.log 오프셋은
T2 트립와이어 판정의 근거**라서 `<num>` 이 되면 사람이 아무것도 판단할 수 없다.
그래서 자릿수를 내리는 대신 **토큰 인접**으로 걸었다.

**③ 의 따옴표를 넓힌 이유 (SR38-4) — 그리고 아래 "남는 구멍" 서술이 틀렸었다는 것.**
예전 규칙은 키 뒤 따옴표를 **큰따옴표 하나만** 받았다(`"?`). 그런데 파이썬
`dict.__repr__` 은 **작은따옴표**를 쓴다 — 트레이스백에 실제로 나오는 꼴이
`{'cash_krw': 90000000}` 이고, 이건 **토큰도 구분자도 있는데 그냥 통과했다.**
즉 아래 문장(*"토큰도 구분자도 없는…"*)이 **실제보다 좁게** 적혀 있었다.
이 문서가 CR40-4 에서 지적받은 것과 **같은 종류의 잘못**(문서가 방어를 과장한다)이라
그대로 적어 둔다. 지금은 `'`·`` ` `` 까지 받고, 자체검사 T7 에 세 형태가 들어 있다.

> **남는 구멍(알고 남긴다 · 오늘 다시 재서 사실만 적는다)**: 토큰도 구분자도 없는
> 8자리 이하 순수 숫자(`95000000`)와 지수 표기(`1.02656e+09`)는 못 지운다 —
> 바이트 수·행수와 구별할 방법이 없다.
> 오늘 그 형태가 알림에 닿는 경로는 **없다**: 알림 문자열은 ① `monitor.sh` 가 만든 것
> (HTTP 코드·건수·바이트·파일모드·인증서일수·ym·경로) ② `job-run.sh` 의 `실패:`/`경고:`
> 두 줄뿐이고, 후자를 만드는 `market-index.sh` 의 `fail()`/`warn()` 에 금액이 없다.
> **`job-run.sh` 가 수집기 같은 다른 배치를 감싸기 시작하는 날** 이 줄을 다시 본다
> (SR37-3 과 같은 방아쇠).

> ⚠️ **세탁 규칙이 아무리 좋아도 나가는 길에서 안 타면 소용없다.**
> 위 시험들은 전부 `scrub()` 을 **직접** 부르는데, 알림이 실제로 나가는 길은
> `send_telegram()` 이다. 거기서 `| scrub` 을 지워도 자체검사 전체가 초록이었다
> (직접 확인했다 — 이 라운드가 스스로 찾은 **10번째 변이**다). 지금은 T7 이
> **`send_telegram` 의 출력**을 DRY-RUN 으로 받아 금액·키·토큰이 살아 나오는지 본다.

그 밖에:
- **로그 원문** — 유출을 감지했을 때도 알림에는 **건수만** 넣는다.
  새어 나온 내용을 알림에 실으면 우리가 다시 유출하는 것이다. SSH 감시도 마찬가지로
  **건수만** 보낸다(사용자명·IP·원문 없음).
- 배치 실패 알림은 우리가 직접 쓴 `실패:` 줄 하나만 보낸다(트레이스백 금지 —
  psycopg 트레이스백은 `DATABASE_URL` 을 비밀번호째로 뱉는다. 주입 시험 O 로 확인).

**⛔ SR38-4 를 고치다 더 나쁜 것을 찾았다 — 같은 결함이 비밀 규칙에도 있었다.**
보안리뷰는 따옴표 구멍을 **금액 규칙 ③** 에서만 봤다. 그래서 ③만 고치고
*"비밀 규칙 ④는 이미 잡는다"* 고 적으려다 **실행해서 확인했더니 아니었다** —
④는 따옴표를 **아예 받지 않아서** 아래 셋이 전부 그대로 통과했다(실측):

| 입력 | 고치기 전 | 고친 뒤 |
|---|---|---|
| `{"api_key": "…"}` (**JSON** — 따옴표가 큰따옴표인데도) | 통과 ⛔ | `<redacted>` |
| `{'TOKEN': '…'}` (파이썬 repr) | 통과 ⛔ | `<redacted>` |
| `{'POSTGRES_PASSWORD': '…'}` | 통과 ⛔ | `<redacted>` |

**금액보다 이쪽이 위험하다.** psycopg·httpx 트레이스백과 dict 덤프가 정확히 그 모양이고,
금액과 달리 비밀은 한 번 나가면 되돌릴 수 없다. ③④ 둘 다 넓혔고 T7 에 다섯 형태를 넣었다.

> 알려진 과잉 세탁(안전한 방향이라 그대로 둔다): `monkey: 12` 처럼 **낱말 끝이 `key`** 인
> 한글/영문 단어 뒤에 `:` 가 오면 값이 `<redacted>` 가 된다. 규칙 ④가 원래 갖고 있던
> 성질이고, 지우는 쪽으로 틀리는 것이라 남긴다(CR42-6 의 `지원 3원격` 과 같은 판단).

> ~~남은 구멍(다음 라운드 · SR36-3/4): `log()` 는 `scrub()` 를 통과하지 않는다~~
> **⛔ 닫았다 (CR-043 라운드). 그리고 이건 '남은 구멍' 이 아니라 이미 열린 구멍이었다.**
> 이 문장은 두 라운드 동안 *"다음 라운드"* 로 남아 있었는데, **피해 경로를 아무도 재 보지
> 않았다.** 재 봤더니 이렇다 — `raise_alert`/`clear_alert` 는
> `log "ALERT $key :: $msg"` 를 **먼저** 찍고 그 다음에 `send_telegram` 을 부른다.
> 그리고 `$msg` 에 **남의 출력**이 들어오는 경로가 실재한다: `job-run.sh` 의
> `사유:`/`경고:` 줄은 **배치가 찍은 아무 문자열**이다. 격리 재현(배치가
> `실패: … postgresql+psycopg://re:<pw>@…` 를 찍게 함):
>
> | 경로 | 결과 |
> |---|---|
> | 텔레그램(DRY-RUN) | `re:<redacted>@` ✅ |
> | **감시 로그** `/var/log/realestate-monitor.log` | **비밀번호 평문** ⛔ |
>
> 이 파일은 0640 root:root 라 오늘 당장 새지는 않는다. 그러나 **이 저장소가 실제로
> 유출을 낸 곳이 바로 로그 파일**이었고(SR32-1 · 0644), `check_logperm` 이 이 파일을
> 감시 대상에 넣어 둔 이유가 그것이다. 게다가 방아쇠가 지금 당겨진다 —
> NEIS·Anthropic 키를 쓰는 배치가 `job-run` 에 감싸이면 그 트레이스백 한 줄로 끝난다.
> → **`log()` 도 `scrub()` 를 통과한다.** 비용은 호출당 sed 1회(한 실행에 1~4줄),
> 결과는 멱등(`<num>`·`<redacted>`·`<token>` 은 어느 규칙에도 다시 안 걸린다).
> 자체검사 T7 이 `log()` 정의를 구조로 못 박는다.

### 알림이 못 나갈 때
- 전송은 3회 재시도 후 포기하고, **성공 표시(`.sent`)를 찍지 않아** 다음 실행에서 다시 시도한다.
- 자격증명이 아예 없으면 `/var/log/realestate-monitor.log` 에 `ALERT-CHANNEL-MISSING` 을 남긴다.
- 그래도 사람에게 닿지 않는다. 그래서 **매일 09:05 요약 1통**이 있다 —
  **아침에 안 오면 감시나 서버가 멈춘 것**이다. 이게 마지막 방어선이다.

---

## 3. 무엇을 감시하는가 · 임계값과 근거

### 5분마다 (`--fast`) — 자원 실측(2026-08-01 수정 후): **maxRSS 13.3MiB · 0.36초**

> 🔴 **배포 상태를 사실대로 적는다 (SR46-2 · 2026-08-05).** 아래 표의 **7d(fail2ban) · 7e(IPv6)**
> 는 **저장소(`deploy/monitor.sh`)에만 있고 아직 서버에서 돌지 않는다.** 실측:
> `grep -c check_fail2ban /opt/realestate/scripts/monitor.sh` → **0**(배치본은 2026-08-04 08:18 판).
> 이 문서가 그 둘을 현재형("돈다")으로 적어 온 것은 **과장이었다** — 이 저장소가 SR-041·SR-042·
> SR45-2 에서 세 번 고친 형태(**문서가 감시 사정거리를 과장**)와 같은 것이다.
> 배포(§1 의 `scp` + sha256 대조)를 마치면 이 문단을 지운다. 그 전까지 v6 방어는
> **ip6tables 규칙과 systemd 유닛 자체**뿐이고, 그것이 사라져도 **알려 주는 것은 없다.**

> 단 **설치 직후 한 번**만 26MiB · 0.58초이다 — `check_sshlogin` 이 43MB 짜리 `auth.log` 를
> 기준값으로 한 번 읽기 때문이고, 그 뒤는 마지막으로 본 바이트 위치부터만 읽는다.

| # | 검사 | 임계값 | 근거 · 오늘 값 |
|---|---|---|---|
| 1 | 메인 `/` 200 · `/api/v1/health` 200 · 지도(무인증) 401 | **2회 연속** 실패 | 404 사고. 1회로 알리면 배포 중 `nginx -s reload` 에 운다. 오늘 200/200/401 |
| 1b | index.html 이 참조하는 `/assets/*.js` 가 **200 + javascript** | 2회 연속 | 이 사이트는 SPA 폴백이라 **없는 경로도 200** 이다(실측) → "없는 경로 404"로는 아무것도 못 잡는다. 반면 `/assets/` 는 폴백에서 빠져 404 가 온다(실측) → 배포 후 root 가 옛 디렉터리를 볼 때 이것만 잡는다 |
| 2 | cgroup `memory.events` 의 `oom_kill` **증가분** | 1건이라도 | OOM 사고. 커널이 세는 값이라 오탐 0. 오늘 db 누적 5 · api 0 |
| 3 | `memory.stat` 의 `anon` | ≥ **130MiB** (한계 192MiB의 68%) | 조기 경고. 오늘 db 2MiB · api 64MiB → 안 운다. **한계: 5분 표본이라 몇 초짜리 급등은 놓친다** — 사후 확실한 증거는 #2 |
| 4 | 루트 디스크 | 여유 < **1.2GiB** 또는 **≥95%** | 오늘 92% · 2.0GiB → 안 운다. 차면 동거 서비스까지 멈춘다 |
| 5 | 파일 모드 — ① `nginx/realestate.{access,error}.log*` ② **`/var/log/realestate*.log*`** | 0640/0600 아님 | 0644 회전본 사고. **설정이 아니라 실제 파일 모드**를 본다. ②는 **우리가 만든 로그**다 — 감시가 자기 산출물을 안 보던 사각지대였다(SR36-2 ③). 오늘 nginx 11개 · 앱/배치 2개 전부 0640(서버 실측) |
| 6 | 로그 안 쿼리스트링 | access 또는 error 에서 1건 | 보안리뷰 지정 신호. `error_log` 는 `log_format` 대상이 아니라 `request: "...?..."` 로 남을 수 있다. 오늘 0/0 |
| 6b | **access 로그 mtime 신선도** | ≥ **24시간** 안 늘면 | ★신규. "파일이 없다"(=#7c)와 "내용이 안 늘었다"는 다르다. **감시 자신이 5분마다 사이트를 4번 찌르므로** 정상이면 하루 1,000줄 이상 쌓인다(실측: 하루치 회전본 195KB) → 24시간 정지는 사람 트래픽과 무관하게 **nginx 가 안 쓰거나 경로가 바뀐 것**이다. 새벽에 조용한 것으로는 절대 울지 않는다 |
| 7 | `/api/` 응답의 **5xx 증가분** | 1건이라도 | "헬스체크는 200인데 지도가 죽는" 형태. 합성 요청은 인증에서 401 로 멈추므로, 실제 인증 요청의 500 은 접근 로그만 안다. **한계: 트래픽이 없으면 신호도 없다** |
| 7b | **`auth.log` 의 `Accepted password`(+`keyboard-interactive`) 증가분** | 1건이라도 | ★신규 · 보안리뷰 트립와이어 **T2**(SR-036R §5). root SSH 위험수용의 조건을 **기계가** 지게 만든 자리다. 우리 접속은 전부 공개키(실측 `publickey 1,250` · `password 0`). **실패 87,269건은 소음이라 세지 않는다.** 43MB 파일을 5분마다 훑지 않도록 **마지막에 본 바이트 위치부터만** 읽는다 |
| 7b' | **`auth.log` 의 변화가 회전으로 설명되는가**(`authshrink`) | 설명 안 되면 | ★개정(SR37-1 → CR42-2 · SR38-1). 옛 코드는 *"줄었으면 로테이션"* 이라 단정해서 침입자의 `: > auth.log`(가장 흔한 흔적 삭제)에 **조용히 0건**을 보고했다. 그 뒤 판정을 두 번 더 뚫렸다: ⓐ`rm auth.log && touch` 는 inode 가 바뀌니 **증거 없이 회전으로 확정**돼 침묵했고, ⓑ`: >` + **`touch .1`** 은 mtime 증거를 **공격자가 만들어** 침묵했다. → 이제 증거를 **위조 비용으로 나눈다**: 강한 증거는 **`.1` 의 inode == 우리가 마지막으로 본 `auth.log` 의 inode**(회전은 `rename(2)` 이라 보존된다 — 맞추려면 진짜로 회전해야 하고 그러면 증거가 `.1` 안에 남는다). mtime 은 **압축 회전본(`.1.gz`)에서만** 쓰고 그때는 `blind_add` 로 *"약한 증거였다"* 를 남긴다. 어느 증거도 없이 파일이 바뀌면 `replaced` — 회전이라고 부르지 않는다. **치른 값**: `copytruncate` 방식 로테이션은 공격자의 위조와 메타데이터가 같아 구별할 수 없다(이 서버는 `create`+`delaycompress` 라 해당 없음 · 경보 문구가 그 가능성을 먼저 적는다) |
| 7b″ | **`auth.log` 가 얼어붙었는가**(`authfresh`) | mtime ≥ **6시간** | ★신규(SR38-3 · 최우선). **공격 없이 고장만으로도 난다** — rsyslog 가 멈추면 파일은 그대로 있고 줄지도 않으므로 7b 는 *"이번 구간 0건 (기대 0)"* 이라고 **적극적으로 무사고를 선언**했다. 그건 "봤고 괜찮다"이지 "못 봤다"가 아니다. 6b(access)와 **같은 모양**인데 이쪽만 없었다. 임계 6시간의 근거: 서버는 `Failed password` 가 88,316건 쌓여 상시 갱신된다 → 오탐 사실상 0. **얼어붙은 동안의 0건은 요약 문구부터 다르게 적는다** |
| 7b‴ | **회전본 `.1` 이 로테이션 없이 바뀌었는가**(`authedit`) | 바뀌면 | ★신규(SR38-2). 회전본은 **만들어진 뒤 변하지 않는다**(다음 회전 때 이름만 밀린다). 그런데 `.1` 은 크기만 보고 내용·정체를 아무도 안 봤다 — 회전 뒤 `sed -i '/Accepted password/d' auth.log.1` 이면 침묵했다. ⚠️ `sed -i` 는 임시파일을 rename 하므로 **inode 가 바뀐다** → *"같은 inode 인데 작아졌다"* 만 봐서는 못 잡는다(직접 확인). 그래서 **정체 변화(회전이 아닌데 `.1` 이 다른 파일)** 도 함께 본다. **한계**: 회전 직후 우리가 `.1` 을 처음 보기 전에 편집하면 못 잡는다(그때는 `.1` 크기 < 오프셋 검사가 잡는데, 지운 양이 마지막 표본 5분 이후 증가분보다 커야 한다) |
| 7c | **위 검사들의 대상이 0개인가** | 하나라도 0개/못 읽음 | ★신규 · fail-open 차단(CR40-2 · §8). 사유를 모아 **한 통**으로 알린다. 이 경보가 켜져 있는 동안 5·6·6b·7·7b 는 "이상 없음"이 아니라 **"모른다"** 이다 |
| 7d-① | **fail2ban 데몬이 `active` 인가** (`f2b_dead`) | **2회 연속** 아니면 | ★신규(2026-08-04 · `fail2ban.md`). 차단을 붙여 놓고 **그 차단을 아무도 감시하지 않았다** — 조용히 죽으면 다시 무방비인데 아무도 모른다. 2회 연속(=10분)을 요구하는 것은 `systemctl restart` 순간에 울지 않기 위해서다. ⚠️ `is-active` 는 inactive 일 때 **낱말을 stdout 에 주고 rc=3** 이다(실측) → rc 로 판정하면 진짜 정지가 '감시불능'으로 덮인다. 반대로 **빈 출력**은 정지가 아니라 못 읽은 것이다(systemd/버스 불가) → `blind_add` |
| 7d-② | **INPUT 에 `f2b-sshd` 점프가 실제로 걸려 있는가**(포트 22 포함 · `f2b_rule`) | 2회 연속 없으면 | ★신규 · **이 칸이 핵심이다.** 서비스는 `active` 인데 **iptables 규칙만 빠질 수 있다** — 도커 재시작 · `iptables -F INPUT` · nft/iptables 백엔드 교체가 그렇게 만든다. 그때 체인(`f2b-sshd`)은 남고 **점프만** 사라지며, 데몬은 아무 이상이 없으니 자기 로그에 `Ban` 을 계속 적는다. **`is-active` 만 보고 "보호되고 있다"고 말하면 그게 거짓말이다.** 실측 기준선: `-A INPUT -p tcp -m multiport --dports 22 -j f2b-sshd`. 포트는 목록·범위(`22,2222` · `20:25`)까지 본다 — 이름만 맞는 규칙에 속지 않는다. 🔴 **그리고 점프의 '위치'까지 본다(SR45-7 · 2026-08-05).** iptables 는 INPUT 을 위에서부터 읽고 ACCEPT 에서 끝내므로, 점프보다 **앞에** 22/tcp 를 통과시키는 ACCEPT 가 있으면 점프는 걸린 채로 **아무 패킷도 못 본다** — 예전에는 그 상태가 "규칙 있음" 초록이었다. 무해한 앞줄(`-i lo` · `--ctstate` 에 NEW 없음 · 다른 프로토콜/포트 · `DROP/REJECT` · `LOG` 류)은 울지 않는다. ⚠️ **하위 체인 안은 못 본다**(`-j ufw-before-input` · `-j RETURN`) → 그건 `blind_add` 로 감시불능. ⚠️ `iptables` 를 **못 부른 것**(권한·xtables 잠금·모듈)은 "규칙 없음"이 **아니다** → `blind_add` 로 감시불능으로 남기고 **`clear_alert` 금지** |
| 7d-③ | **누적 차단수가 늘고 있는가** (`f2b_stale`) | **24시간** 동안 0 증가 | ★신규. 서비스도 살아 있고 규칙도 걸렸는데 **필터가 로그를 안 읽는** 상태가 있다(logpath 어긋남 · 로테이션 뒤 재열기 실패 · 정규식이 이 sshd 버전과 안 맞음). 창의 근거는 **실측**이다: `auth.log` 를 `maxretry=5`/`findtime=10m` 규칙으로 전수 재생하면 **하루 235~517건**(10일 연속 · 최소 235)이 차단 조건에 닿고, 실가동 2시간 25분의 실제 차단은 22건(≈9건/시간)이었다. **24시간 0건은 이 서버에 없다.** 반대로 **5분 0건은 완전히 정상**이라 창을 짧게 잡으면 5분마다 운다. ⚠️ 데몬 재시작은 카운터를 0 부터 다시 세게 한다 → **감소는 리셋으로 보고 기준값만 다시 잡되 `clear` 는 하지 않는다**(리셋은 회복의 증거가 아니다 — 진짜 증가가 곧 꺼 준다) |
| 7d-④ | 차단 **건수** 자체 | (경보 아님) | 하루 수백 건이 정상이라 건수로 울면 그날로 늑대소년이 된다. 요약에 **사실로만** 싣는다(현재 차단 · 누적 차단 · 이번 구간 증가분 · 필터 누적 실패). ⚠️ 알림·요약에 **IP 는 싣지 않는다** |
| 7e-① | **`ip6tables` INPUT 에 tcp/22 를 막는 DROP 이 걸려 있는가** (`v6ssh_rule`) | 2회 연속 없으면 | ★신규(SR-046 · 2026-08-05 · `fail2ban.md` §IPv6). **fail2ban 은 v6 를 못 막는다** — `<HOST>` 정규식이 링크로컬 존 접미사(`fe80::…%enp3s0`)를 못 읽어 **탐지가 0** 이다(실측 `fail2ban-regex` `0 matched, 1 missed`). 그런데 위 7d-② 는 **`iptables`(v4) 만** 보므로 `sshd` 가 `[::]:22` 를 듣고 `ip6tables` 가 `-P ACCEPT` 뿐이던 동안에도 **초록이었다**. 실측 기준선: `-A INPUT -p tcp -m tcp --dport 22 -j DROP`. ⛔ **정상 출력에는 fail2ban 이 만든 v6 `f2b-sshd` 점프가 이미 들어 있다** — 그러니 v6 에서 "점프가 있으니 보호된다"는 **거짓**이고, 이 검사는 점프가 아니라 **DROP** 을 본다. 이름만 맞는 규칙에 안 속는다: **출발지가 붙은 DROP(`-s`)·`-i lo` 전용·다른 포트·udp·맺힌 연결만**은 우리 보호로 세지 않는다. 순서도 7d-② 와 같은 규율로 본다(DROP 보다 **앞의** ACCEPT 는 DROP 을 무력화한다 · 하위 체인 안은 `blind_add`). ⚠️ `ip6tables` 가 **없거나 실행 실패**면 "규칙 없음"이 아니라 **감시불능**이다(v6 를 안 쓰는 호스트가 있다) → `clear_alert` 금지 |
| 7e-② | **`re-v6-ssh-drop.service` 가 `enabled` 인가** (`v6ssh_unit`) | 2회 연속 아니면 | ★신규. 이 서버에는 `ip6tables-persistent` 가 없어 **규칙이 재부팅에 사라진다** — 다시 거는 것이 이 oneshot 유닛 하나뿐이다. ⛔ **`is-active` 를 보호의 증거로 쓰지 않는다(실측하고 정했다)**: `Type=oneshot`+`RemainAfterExit=yes` 라 부팅 때 한 번 성공하면 **규칙을 손으로 지워도 영원히 `active`(exited)** 다. 그래서 기준은 `is-enabled` 이고, `inactive` 자체는 경보가 아니다(`ExecStop` 이 없어 stop 해도 규칙을 안 걷는다). 별도로 `is-active=failed`(enabled 인데 마지막 실행이 실패)도 경보다 — 재부팅해도 규칙이 안 걸린다. 그때는 규칙 경보를 **보류**해 한 사고에 두 통을 안 보낸다. 🔴 **함정**: 유닛 파일을 지우면 `is-enabled` 가 **빈 출력 + rc=1** 인데, 그것은 systemd 를 못 읽었을 때와 **모양이 같다**. `is-active` 가 낱말을 주면(없는 유닛에도 `inactive` 를 준다) 버스는 살아 있다는 뜻이므로 **유닛 부재로 판정해 경보**하고, 둘 다 비면 **감시불능**이다. 이 구분을 안 하면 사람이 유닛을 지운 사고가 조용히 묻힌다 |
| 7e-③ | v6 무차별 대입 **건수** | (경보 아님 · **애초에 못 센다**) | 우리는 포트를 통째로 닫았을 뿐이고 fail2ban 의 v6 탐지는 **여전히 0** 이다. 규칙이 빠져 있던 동안 무슨 일이 있었는지는 `auth.log`(#7b)로만 알 수 있다. **없는 사정거리를 있는 척하지 않는다** |
| 8 | 일일 감시가 살아 있는가 | 30시간 이상 안 돌면 | 상호 감시 |

### 매일 09:05 (`--daily`) — 위 전부 + 아래 · 최대 RSS 26.5MiB · 0.58초

| # | 검사 | 임계값 | 근거 |
|---|---|---|---|
| 9 | 인증서 남은 일수 (**4장 전부 — 동거 서비스 포함**) | < **21일** | certbot 은 30일에 갱신한다 → 21일이면 갱신이 여러 번 실패한 것. 3장이 `installer = nginx` 라 **우리 nginx 설정이 나쁘면 동거 서비스 갱신이 실패**한다. 그 실패는 그쪽 만료로만 드러난다. 오늘 최단 34일(data) |
| 9b | **인증서 대상이 0개인가** | 0개면 | ★신규(CR42-3). CR40-2 가 로그 검사 3종에서 막은 **fail-open 이 여기엔 그대로 남아 있었다**: `$LE_DIR` 밑에 `cert.pem` 이 없으면 루프를 한 번도 안 돌고 `worst=9999` 로 내려와 **`clear_alert cert` 를 불렀다** — `ALERT-CLEARED cert :: 인증서 여유 회복 (최단  9999일)`(이름 빈칸 · 존재하지 않는 값)이 나가고 켜져 있던 경보가 지워졌다. `blind_add` 도 없어 `logblind` 로도 안 걸렸다. 이제 **대상 수를 세고 0개면** ①요약을 *"검사 못 함 (대상 0개)"* 로 ②`blind_add` ③**`clear_alert` 금지**. 일부만 못 읽어도 "여유 회복"이라 말하지 않는다. ⚠️ `check_cert` 가 `blind_add` 를 하므로 `--daily` 실행 순서에서 **`check_logblind` 보다 앞**이어야 한다 |
| 10 | `listing` 신규 컬럼 2/2 · `listing_user_*` CHECK 7/7 | 하나라도 어긋나면 | 016 누락 = 지도·추천 통째로 죽음(DEPLOY.md §5-3b). DB 연결 **하루 1개**만 쓴다 — 연결 하나가 곧 백엔드 하나(메모리)라 5분 경로에서는 절대 안 한다. 오늘 2/2 · 7/7 |
| 11 | 시장지수 기준월이 기대월보다 이전인가 (**별도 키 `marketstale`**) | 이전이고 **이번 달 배치 기록이 없으면** | **기대월은 "오늘"이 아니라 "마지막 배치 시점"으로 잰다**(§8-1). 배치는 매월 1일 04:10 에만 돌기 때문이다. 기대월 = (마지막 배치일 − 30일)이 속한 달의 직전 달. 오늘 실제 2026-06 = 기대 2026-06.<br>★개정(CR42-1 · SR38-8) — 예전에는 이 항목이 **`dbstruct` 키를 10·11b·시장지수 0행과 공유**했고 쿨다운 86400 이라 **밀린 날마다 한 통**이 갔다. 완결 0인 달이 끼면 그게 한 달 내내다. 이제 ①**별도 키**로 떼고 ②`$JOBS/market-index.status` 를 읽어 **이번 달에 배치가 돌았으면 보내지 않는다**(그건 배치가 이미 한 말이다 — 감시는 *"배치가 아예 안 돌아 아무 신호도 없는 경우"* 만 운다). 쿨다운 7일. **느슨해진 것이 아니다**: 배치 기록이 없거나·지난달이거나·값이 깨졌으면 전부 "안 돌았다"로 본다(fail-closed) |
| 11b | 진행 중인 달을 `is_complete` 로 표시한 행 | 1건이라도 | CR33-1 재발 감지. 오늘 0 |
| 12 | DB 컨테이너 로그의 크래시 복구 흔적(24시간) | 1줄이라도 | #2 보강. 호스트 OOM killer 등 다른 원인도 여기 남는다. 오늘 0 |
| 13 | 컨테이너 json 로그 크기 | ≥ 300MiB | `/etc/docker/daemon.json` 이 없어 **회전 설정이 없다**(실측). 오늘 db 2MiB · api 0MiB |
| 14 | 5분 감시가 살아 있는가 | 20분 이상 안 돌면 | 상호 감시 |
| 15 | 배치 상태 요약 | (경보 아님) | 즉시 경보는 `job-run.sh` 가 한다 |

> **신선도의 1차 방어는 감시가 아니라 배치 자신이다.** `market-index.sh` 가 자기가 만든
> 기준월을 그 자리에서 기대값과 대조하고, 어긋나면 0 이 아닌 코드로 끝난다 →
> `job-run.sh` 가 즉시 알린다. #11 은 **배치가 아예 안 돌아 아무 신호도 안 나는 경우**를
> 잡는 2차 방어다. 두 자리가 같은 함수(`market_expected_ym`)를 쓰고, 그 두 사본이
> 갈라지지 않는지는 `monitor-selftest.sh` T3 가 대조한다.

### CR-043 라운드에서 5분 검사에 더한 것 (2026-08-01)

| # | 검사 | 임계 | 근거 |
|---|---|---|---|
| 7b-② | **`auth.log` 의 mtime 은 새것인데 크기가 그대로인가** (`authfake`) | 즉시(쉼도 6시간) | ★신규 SR39-2/SR39-1. append-only 로그는 **쓰면 커진다** — "mtime 이 앞으로 갔는데 한 바이트도 안 늘었다"는 정상 운영에 없다. 만드는 길이 둘뿐이다: ①`touch` 로 **신선도 증거만 위조**(로그는 얼어 있다 — ⓪ 이 mtime 하나만 보던 자리) ②이미 쓰인 구간을 **같은 길이로 덮어쓰기**(침입 줄을 평범한 줄로 — 크기·inode 가 **둘 다 보존**된다). ⚠️ 오탐 0의 조건이 **크기·inode·mtime 을 한 번의 `stat` 으로** 읽는 것이다. 따로 읽으면 그 사이의 정상 쓰기가 같은 모양을 만든다(서버는 `Failed password` 가 시간당 ~100건이라 그 창이 실제로 열려 있다). ⛔ **사정거리를 과장하지 않는다(SR40-3 · 2026-08-02 실측)**: 조건이 `크기 == 직전 오프셋` 이라 **창 안에 auth.log 가 한 바이트도 안 늘었을 때만** 발동한다. 그런데 이 서버는 5분 창이 **항상** 자란다 — 즉 ②번(같은 길이 덮어쓰기)은 **운영 조건에서 여기 안 잡힌다**(`how=grown`). 그것을 잡는 것은 아래 7b-③ 교차 하나뿐이고, 그래서 7b-④ 가 그 교차의 **실효성**을 따로 본다. 여기 남는 실효 범위는 ①`touch` 신선도 위조와 ②로그가 얼어 있는 동안의 덮어쓰기다 |
| 7b-③ | **journald 가 같은 구간에서 비밀번호 로그인 성공을 보는가** (`sshpw` 본문) | 어느 쪽이든 >0 | ★신규 SR39-1. 위까지의 증거는 전부 **메타데이터**인데, 공격자가 바꾸는 것은 *아직 읽지 않은 구간의 내용*이다. 특히 직접 `mv` 로 회전시켜 **강한 증거를 성립시킨 뒤** `.1` 을 덮어쓰면 그것도 안 보인다 — `rename(2)` 은 **정체만** 보존하지 내용을 보존하지 않는다(옛 주석이 뒤쪽까지 증명한다고 적은 것은 과장이었다). → **출처를 하나 더 본다.** 서버에 영구 journald 가 있다(실측: `/var/log/journal` 아카이브 포함 2.0G · 24시간 sshd `Accepted` 258건). 비교가 아니라 **각자 센다** — `--since` 가 초 단위라 창 경계에서 두 수가 정상적으로 1 어긋날 수 있고 그 차이를 경보로 쓰면 오탐이 된다. 수가 어긋난 사실은 같은 알림 본문에 문장으로 싣는다 |
| 7b-④ | **두 번째 출처가 쓸모없어졌는가** (`sshjournal`) | ①응답이 멈췄을 때 ②**응답은 하는데 ssh 메시지를 0줄 줄 때**(창 ≥120초 · 같은 창의 auth.log sshd 줄 ≥2) | ★신규. **한계를 정직하게 적는다** — journald 도 root 면 지운다(`journalctl --rotate --vacuum-time=1s`). 이 교차는 침입을 불가능하게 만들지 않고 **두 곳을 다 지워야 하게** 비용을 올릴 뿐이다. 그러면 그 비용을 치렀다는 사실만큼은 우리가 알아야 한다. journald 가 없는 호스트에서는 아무 일도 하지 않는다(요약에 "교차 불가" 만 적는다). ⛔ **그런데 “사라졌는가” 만 보는 것은 부족했다(SR40-1)** — 그래서 `jtot` 을 넣었다. 🔴 **다만 그 조치가 `--vacuum-time` 을 막는다고 적었던 것은 과장이었다(SR41-1 · 2026-08-03 실측 정정).** 위 괄호가 스스로 모순을 적고 있었다: *“새 메시지가 즉시 들어온다”* 면 `jtot` 은 **0 이 되지 않는다.** 실측(이 서버): ssh 유닛 24시간 **37,632줄 · 평균 간격 2.30초 · 최장 무음 104초**(≥120초 0회), `--since @(now-3s)` 가 이미 **4줄**. 즉 저널을 지워도 창은 몇 초 만에 다시 차고 **무작위 vacuum 의 탐지 확률은 약 4.4%**, 공격자가 지운 직후 접속을 한 번만 만들면(로그아웃만 해도 ssh 유닛 2줄) **0%** 다. → **이 교차가 지키는 것은 “앞으로 계속 눈멀어 있는 상태”이지 “과거를 지웠다”가 아니다.** 잡는 것: `Storage=none` · ssh 유닛 이름 변경/소켓 활성화 · 저널 삭제·권한박탈이 **지속될 때** · `journalctl` 응답불가. **못 잡는 것: 1회성 `--vacuum-time`/`--rotate` · vacuum 뒤 ssh 이벤트 1회 · 저널 파일 삭제 후 재기동 · 자기 세션 줄만 제거.** 이 칸은 **x9(같은 길이 덮어쓰기)에 대한 방어로 계산하지 말 것** — 운영 조건에서 그것을 잡는 수단은 지금 없다. 같은 상태를 만드는 길이 더 있다: `Storage=none` · ssh 유닛 이름 변경/소켓 활성화(`ssh@0-….service` 는 `-u ssh` 에 안 걸린다) · 저널 손상. → 이제 **창 안의 ssh 유닛 메시지 총량(jtot)** 을 함께 세고, 그것이 0 인데 같은 창의 auth.log 가 sshd 줄을 받았으면 **모순**으로 보고 경보한다. **오탐 0 의 근거(2026-08-02 서버 실측)**: 24시간 ssh 유닛 메시지 29,195줄 → **5분 버킷 288개 중 0줄 버킷 0개**(최소 21 · 평균 101). 다만 **1분 버킷은 1,440개 중 30개가 0줄**이라 창 길이 문턴(120초)을 둔다. 그리고 auth.log 는 sshd 말고도 자라서(CRON · systemd-logind · systemd-user 실측) "auth.log 가 자랐다" 말고 **그 구간의 sshd 줄 수**를 본다. ⚠️ **해소도 붙였다(CR44-1)** — 예전에는 clear 경로가 아예 없어 한 번 뜨면 일일 요약 머리말이 영영 `미해소 (sshjournal)` 이었다. 단, **교차가 실제로 돌아왔을 때만**(응답 O + 그 창의 ssh 메시지 >0) 끔다 — 응답만 하고 0줄인 상태에서 끄면 그것이 CR40-2 가 막은 거짓 해소다 |
| 8b | **일일 점검이 `한 번도` 안 돌았는가** (`daily_dead`) | 첫 5분 실행 후 30시간 | ★신규(15번째 자리). 같은 함수 안에서 대칭이 깨져 있었다 — daily 쪽은 *"5분 감시가 한 번도 돈 기록이 없다"* 를 경보로 만드는데 fast 쪽은 같은 상황에서 조용히 `return 0` 했다(실측: `--fast` 8회에 `daily_dead` **0건**). 그러면 `--daily` 크론 한 줄이 빠졌을 때 **인증서·DB구조·시장지수·DB크래시·컨테이너로그 일곱 검사**가 통째로 없는 채로 아무 신호가 없고, 드러나는 유일한 경로가 *"아침 요약이 안 온다"* 는 **사람의 기억**이다. 위험은 **크론 한 줄이 사라지거나 다음에 다시 설치할 때** 열린다(크론 자체는 2026-07-30 에 이미 들어갔다 — CR44-4) |
| 7b-⑤ | 추적하던 **평문 `.1` 이 사라지고 `.1.gz` 만** 남았는가 (`authedit`) | 즉시 | ★신규 SR39-3. 이 서버는 `delaycompress` 라 회전하면 `.1` 이 `.2` 로 밀린다 — **`.1.gz` 라는 상태가 없다**. `: > auth.log` + 가짜 `.1.gz` 가 정확히 그 모양인데 예전에는 약한 증거(`rotated_gz`)로 **세어 주고** 넘어갔다. ⚠️ 오탐 0의 근거는 조건이 **설정이 아니라 관측**이라는 것이다: `compress` 서버는 평문 `.1` 을 애초에 본 적이 없어 이 판정에 들어오지 못한다(자체검사에 그 대조군이 있다) |
| 7b-⑥ | **`journalctl` 이 "해당 없음"을 빈 출력으로 주는가** (교차 판정 보류 · `logblind`) | 매 실행 (미래 60초 창 프로브 · 실측 0.01초) | ★신규 CR45-1. **7b-④ 의 눈멂 탐지는 전부 `jtot == 0` 에 걸려 있고, 그 `0` 은 `-o cat` 이 "해당 없음"을 0바이트로 준다는 데 걸려 있다.** 실측(2026-08-03 · 이 서버 systemd 249): `journalctl -u ssh --since @<미래> -o cat` → **0바이트** / 같은 질의를 `-o cat` 없이 → **`-- No entries --` 가 stdout 으로** 나온다. 즉 누가 디버깅하려고 `-o cat` 을 빼거나 systemd 가 형식을 바꾸면 `jtot` 이 늘 1 이상이 되어 **7b-④ 가 영원히 발동하지 않는다** — 그리고 자체검사는 **가짜 `journalctl`** 을 쓰므로 그 상태에서도 전부 초록이다. ⛔ 이것이 이 저장소가 반복해 온 형태(**픽스처가 현실이 아니라 우리 가정대로 굴어서 통과한다**)의 정확한 재발 지점이었다. → 가정하지 않고 **매 실행 확인한다**: 반드시 비어야 하는 창(1시간 뒤 60초)을 물어서 그것이 안 비면 우리는 0줄을 판정할 능력이 없다고 보고 **판정을 보류한다** (경보도 안 하고 **해소도 안 한다** — 못 믿는 수로 끄면 그게 CR40-2 가 막은 거짓 해소다). 자체검사에는 `-- No entries --` 를 뱉는 **현실 쪽으로 거짓말하는 가짜**(교차 j3)를 따로 둔다 — 프로브를 무력화하는 변이를 심으면 그 4건이 붉어진다(실측) |

### 알림 정책
- **상태형**(웹·번들·디스크·로그권한·로그유출·로그신선·감시불능·anon·인증서·DB구조) → 쿨다운 **6시간**
  (인증서·DB구조는 24시간). 같은 문제로 5분마다 울면 아무도 안 본다.
- **사건형**(OOM kill · 배치 실패 ) → 쿨다운 **0**. 새 사건은 새 정보다.
  - ⛔ **정정(CR47-3 · 2026-08-03)**: `SSH 비밀번호 로그인 성공`(`sshpw`)은 이제 **900초**다. 쿨다운 0 이면 5분마다 = **하루 288통**이고, 원격에서 켤 수 있었던 전력(SR41-2)을 감안하면 그 자체가 채널을 죽이는 길이다. **첫 통은 여전히 즉시 나간다**(`.sent` 가 없으므로) — 900 은 *재통보* 주기다.
- API 5xx → 1시간 (봇이 깨진 엔드포인트를 두드릴 때 폭주 방지).
- 문제가 풀리면 **해소 1통**을 보낸다(사람이 "아직 켜져 있나"를 몰라도 되게).
  단 **검사를 실제로 수행했을 때만** 보낸다 — 못 본 것을 "해소됐다"고 통보하지 않는다(§8-2).

---

## 4. 일부러 뺀 것 — 오탐이 결함이기 때문

| 뺀 것 | 이유 |
|---|---|
| `pg_postmaster_start_time()` 변화 | **크래시 복구 때 변하지 않는다.** 실측: 값 `2026-07-26 04:07:59` = 컨테이너 기동 시각인데, 7/27·7/28 에 재초기화가 3번 있었다. postmaster(PID 1)는 죽지 않고 백엔드만 죽기 때문이다. **이 검사는 실제 사고 3건을 전부 놓쳤을 것이다.** → #2 로 대체 |
| `docker stats` 의 MEM USAGE / MEM % | page cache 를 포함해 정상적으로 한계(192MiB)에 닿는다. 매일 우는 검사 → `anon` 으로 대체 |
| `memory.events` 의 `max` 카운터 | 오늘 이미 152,189 이다. 한계에 닿는 것 자체는 정상 |
| 호스트 `free` 메모리 경보 | 여유 234MiB 가 정상 상태다. page cache 로 흔들리고, 동거 서비스 메모리에 우리가 손댈 수도 없다 → 요약에만 넣고 알리지 않는다 |
| CPU / load / 응답시간 SLO | 사고 이력이 없고 **정상 기준선을 모른다**. 모르는 임계값은 안 만든다 |
| 컨테이너 재시작 감시(`RestartCount`) | OOM 때 재시작이 **안 됐다**(RestartCount=0). 이 신호로는 안 보인다 |
| **SSH 로그인 실패 건수** | 87,269건이다. 세면 매번 운다. 그래서 **성공만** 센다(#7b) |
| journald 2.0GiB · `btmp` 333MiB 증가 감시 | 우리 것이 아니고 일회성 정리로 끝나는 문제다 → §6 사용자 작업으로 보고 |
| **fail2ban 차단 건수** | 실측 하루 **235~517건**이 정상이다. 그것으로 울면 진짜 신호(**보호가 꺼졌다**)가 함께 묻힌다 → 요약에 사실로만 싣고, 경보는 "보호가 꺼졌다"·"필터가 로그를 못 읽는다"에만 건다(#7d) |
| `f2b-sshd` 체인 **안**의 차단 규칙 수 | 0개가 정상일 수 있다(차단 중인 IP 가 마침 없을 때). 정상과 고장을 구별할 기준선이 없으므로 보지 않는다 — 대신 **INPUT 의 점프**(#7d-②)를 본다 |
| 동거 서비스 헬스체크 | 우리 소관이 아니다. 단 **인증서는 본다**(우리가 깨뜨릴 수 있어서) |

---

## 5. 설치 중 새로 드러난 사실 2건

1. **DB OOM 은 보고된 2회가 아니라 최소 3회 + 커널 카운터는 5건이다.**
   `memory.events` 의 `oom_kill = 5`(7/26 컨테이너 기동 이후 누적), 컨테이너 로그에는
   7/27 12:47 · 7/27 21:51 · 7/28 11:32 세 번의 `terminated by signal 9` → `reinitializing`
   → `database system is ready` 가 남아 있다. 그런데 `docker ps` 는 지금도
   `Up 4 days (healthy)` 이고 `RestartCount=0` 이다. **`docker ps` 로는 절대 안 보인다.**
2. **`127.0.0.1:8000` 은 우리 API 가 아니다 — `itsmine-engine` 이다.**
   우리 API 는 `127.0.0.1:8013` 이다. 8000 을 찌르면 FastAPI 가 응답하므로 200/404 가
   그럴듯하게 돌아온다 → **남의 서비스를 감시하면서 우리가 살아 있다고 착각**하게 된다.
   `monitor.sh` 는 8013 을 쓴다.

참고로 컨테이너의 `memswap_limit: 192m` 은 실제로 스왑을 막지 못하고 있다
(`memory.swap.max = max`, db 가 지금 68MiB 를 스왑에 두고 있다). 스왑이 있어서
OOM 이 덜 나는 쪽이라 지금은 이득이지만, **설정과 실제가 다르다**는 사실은 남겨 둔다.

---

## 6. 사람이 해야 할 일 (감시로 대신할 수 없는 것)

| 우선 | 할 일 | 왜 |
|---|---|---|
| 높음 | **디스크 확보** — `journalctl --vacuum-size=200M` 으로 약 1.8GiB 회수 가능. ⚠️ **journald 는 이제 T2 의 두 번째 출처다(§7b-③)** — `--vacuum-time=` 계열은 **쓰지 말 것**(창 안의 ssh 기록을 통째로 지워 교차를 무력화한다). `--vacuum-size` 는 **1GiB 이상 남길 것**. `--vacuum-size=200M` 자체는 안전하다(24시간 ssh 유닛 29,195줄 기준 5분 창은 넘치게 남는다 — 재서 확인했다). `docker image prune` 대상 7.5GiB(단, 동거 서비스 이미지가 섞여 있어 `-a` 는 위험) | 92% 다. 차면 동거 서비스까지 멈춘다. 우리 것이 아니라 **실행은 사용자 판단**으로 남겼다 |
| 높음 | **DB OOM 근본 원인** — `mem_limit 192m` 에서 어떤 쿼리가 192MiB 를 먹는지. `work_mem`/`shared_buffers` 와 §5-1 의 무거운 조회를 함께 봐야 한다 | 감시는 "또 죽었다"를 알려줄 뿐 안 죽게 만들지 못한다 |
| 중간 | `/etc/logrotate.d/realestate` 에 `create 0640 root adm` | 배치 로그는 지금 `job-run.sh` 가 매 실행마다 0640 으로 맞춘다(+크론 `umask 027`). 로테이션까지 걸면 셋째 겹이 된다 |
| 중간 | `/etc/docker/daemon.json` 에 `log-opts max-size` | 컨테이너 로그 회전 설정이 없다 |
| 낮음 | 우리 전용 텔레그램 봇 분리(`/etc/realestate-monitor.env`) | 지금은 pjt12-adsense 의 봇에 얹혀 있다 |
| 낮음 | 외부 dead-man 스위치(healthchecks.io 등) | 서버가 통째로 죽으면 우리 감시도 같이 죽는다. 지금은 "아침 요약이 안 옴"으로만 안다 |
| 낮음 | SSH 브루트포스 — `btmp` 333MiB · fail2ban | 위험수용(SR-036R) 상태다. **성공** 흔적은 #7b 가 본다 |

---

## 7. 운영 명령

```bash
# 지금 상태를 화면으로 보기 (알림 안 보냄)
RE_MON_DRY_RUN=1 RE_MON_PRINT=1 /opt/realestate/scripts/monitor.sh --fast
RE_MON_DRY_RUN=1 RE_MON_PRINT=1 /opt/realestate/scripts/monitor.sh --daily

# 감시 스크립트 자체의 회귀 검사 (네트워크·운영 상태를 안 건드린다. 알림 0통)
bash /opt/realestate/scripts/monitor-selftest.sh

# 알림 경로만 시험 (실제로 1통 간다)
/opt/realestate/scripts/monitor.sh --test-alert

# 감시 로그 · 지금 켜져 있는 경보
tail -50 /var/log/realestate-monitor.log
ls /var/lib/realestate-monitor/alerts/*.active 2>/dev/null

# 경보를 손으로 끄기 (원인을 고친 뒤에만)
rm -f /var/lib/realestate-monitor/alerts/<키>.active /var/lib/realestate-monitor/alerts/<키>.sent

# 배치를 손으로 돌리기 (실패하면 알림이 온다)
/opt/realestate/scripts/job-run.sh market-index -- /opt/realestate/scripts/market-index.sh
```

### 실패 주입 시험 — **격리 상태에서만** 한다
⚠️ 운영 상태 디렉터리로 주입하면 **사용자 텔레그램으로 진짜 알림이 간다**(2026-07-31 에
그렇게 6통이 갔다). 아래는 전부 임시 상태 디렉터리 + DRY-RUN 이라 한 통도 안 나간다.

```bash
export RE_MON_STATE=/tmp/t RE_MON_LOG=/tmp/t.log RE_MON_DRY_RUN=1
rm -rf /tmp/t /tmp/t.log; M=/opt/realestate/scripts/monitor.sh

$M --fast                                        # 1회차: 기준값
RE_MON_URL_MAIN=http://127.0.0.1:9/ $M --fast    # 사이트 다운 (1회차는 침묵해야 정상)
RE_MON_URL_MAIN=http://127.0.0.1:9/ $M --fast    # 2회차 → 경보
echo 3 > /tmp/t/kv/oomkill_realestate-db; $M --fast    # OOM 2건 주입 → 경보
RE_MON_DISK_MIN_KB=99999999 $M --fast            # 디스크 → 경보
RE_MON_ANON_MAX_MB=1       $M --fast             # anon → 경보
RE_MON_LOG_DIR=/tmp/none   $M --fast             # 로그 대상 0개 → **감시불능** 경보
RE_MON_ACCESS_LOG=/tmp/none.log $M --fast        # access 로그 없음 → 감시불능 경보
RE_MON_AUTH_LOG=/tmp/fake.auth $M --fast         # (Accepted password 줄을 넣어 두면) SSH 경보
RE_MON_CERT_MIN_DAYS=999   $M --daily            # 인증서 → 경보
RE_MON_PG_DB=nosuchdb      $M --daily            # DB 구조 조회 실패 → 경보
grep -E "ALERT " /tmp/t.log
```

> 위 목록이 **지금 재현 가능한 전부**다. 설치 당시 *"13가지"* 라고 적었는데 목록이 없어
> 다음 사람이 재현할 수 없었다(CR40-10) — 그래서 세는 대신 **명령을 적는다**.
> 그리고 임계값·URL 을 바꾸는 이 방식만으로는 **"대상 파일이 아예 없는 경우"** 를 못 만든다.
> 그게 §8-2 의 결함을 못 잡은 이유이고, 지금은 위 3줄(`RE_MON_LOG_DIR`/`ACCESS_LOG`/`AUTH_LOG`)과
> `monitor-selftest.sh` 가 그 자리를 덮는다.

---

## 8. 감시 자신이 낸 결함 2건 (2026-08-01 수정 · CR-040)

감시를 만들 때 세운 원칙 두 개를 **감시 자신이 어겼다**. 기록으로 남긴다.

### 8-1. `CR40-1` — "배치가 안 돌았다"는 거짓말 (오탐)

기대 기준월을 **오늘 날짜**로 계산했는데, 배치는 **매월 1일 04:10 에만** 돈다.
DB 값은 한 달 내내 고정인데 기대값만 날마다 앞으로 가서, 배치가 정상인데도 울었다.

* **실제로 울었다** — 2026-07-31 09:05:06, `시장지수 기준월 2026-05 < 기대 2026-06(월배치가 안 돌았다)`.
  그 시각 배치는 아무 문제가 없었다(8/1 04:10 에 정상 실행 · 51초 · 2,381행).
* 그대로 두면 **한 해 35일**, 2027-03 은 **3/3~3/31 29일 연속**이다.
* 하필 **배치 미실행을 잡는 경보가 이것 하나뿐**이라, 늑대소년이 되면 진짜 실패가 묻힌다.

**고친 방식**: 기대월을 **마지막 배치 시점**(이번 달 1일 04:10, 아직 안 왔으면 지난달 1일)
기준으로 계산한다. 그리고 **1차 단언을 배치 자신에게** 옮겼다 — `market-index.sh` 가 자기
결과를 그 자리에서 대조하고 어긋나면 실패로 끝난다. 감시는 그 뒤를 본다.
`monitor-selftest.sh` T2 가 **365일을 하루씩 돌려 오탐 0**, 옛 공식은 **35일 오탐**임을
매번 확인한다(되돌리면 검사가 죽는다).

### 8-2. `CR40-2` — 대상이 없어도 "이상 없음" (fail-open)

로그 권한·로그 유출·API 5xx 세 검사가 **대상 파일이 하나도 없을 때** 이렇게 보고했다:

```
로그권한: 0개 검사 · 이상 없음     ← 아무것도 안 봤다
로그유출: 0건 / 0건                ← grep 이 실패해 0 이 된 것이다
API 5xx : 0건                      ← 기준값까지 0 으로 덮었다
그리고 clear_alert 까지 호출 → **켜져 있던 경보에 거짓 해소 통보**
```

하필 이 셋이 **이 프로젝트가 실제로 유출을 냈던 경로**(0644 회전본 · 쿼리스트링)를 지킨다.

**고친 방식**: 대상이 0개거나 못 읽으면 ① 요약에 *"검사 못 함"* 으로 적고
② `clear_alert` 를 **부르지 않고** ③ 사유를 모아 `감시불능` 경보 한 통을 보낸다.
`check_api5xx` 는 기준값도 건드리지 않는다(0 으로 덮으면 파일이 돌아오는 순간
누적 전체가 델타로 잡혀 폭증 경보가 난다).
겸해서 **access 로그 mtime 신선도**(#6b)를 넣어 *"파일은 있는데 아무것도 안 쓰이는"* 자리를 메웠다 —
단 **"파일이 없다"와 "내용이 안 늘었다"는 끝까지 구분**한다. 새벽에 사람이 없어서
조용한 것으로는 울지 않는다(우리 감시 자신이 5분마다 로그를 남기기 때문에 성립한다).

### 8-3. 같이 처리한 것
* **`SR36-2`** — 배치 로그 0644 → ① 지금 파일 `chmod 640` ② `job-run.sh` 가 **매 실행마다** 맞춤
  + 크론 `umask 027` ③ **`check_logperm` 대상에 `/var/log/realestate*.log*` 추가**(③이 본체 —
  감시가 자기 산출물을 안 보던 사각지대).
* **`SR36-1` T2** — `check_sshlogin` 신설(#7b). 위험수용의 조건을 사람의 기억이 아니라 기계가 본다.
* **`CR40-4`/`SR36-3`** — `scrub()` 의 금액 규칙을 고치고 **문서(§2)를 코드와 같은 말로** 바꿨다.
* **`CR40-3`** — 셸 4종에 자동 검사가 0건이던 것 → `monitor-selftest.sh`(§9).

### 8-3b. 덤으로 찾은 것 — **같은 코드가 환경에 따라 다른 결과를 낸다**

> ⚠️ **2026-08-01 정정(CR41-5)**: 처음 이 절은 *"C.UTF-8 에서는 `grep 'A.*B'` 가
> 사이에 한글이 끼면 매치하지 않는다"* 고 적었다. **그 진단은 틀렸다.** 다시 재 보니
> 줄이 **정상 UTF-8** 이면 `C`·`C.UTF-8`·`en_US.UTF-8`·`ko_KR.UTF-8` 넷 다 **매치한다**.
> 조치(로케일 고정)와 범위는 옳았지만 근거가 틀렸으므로 아래를 사실로 바꿔 적는다 —
> 다음 사람이 원인을 잘못 알고 되돌리면 안 된다.

실제 원인은 **유효하지 않은 바이트**다. 로그 한 줄에 UTF-8 로 해석되지 않는 바이트가
섞이면 UTF-8 로케일의 정규식은 그 줄에서 **조용히 매치를 놓친다**. 실측(같은 입력·같은 패턴):

| 입력 | `LC_ALL=C` | `C.UTF-8` | `en_US.UTF-8` | `ko_KR.UTF-8` |
|---|:--:|:--:|:--:|:--:|
| 정상 UTF-8(한글 포함) | 1 | 1 | 1 | 1 |
| 유효하지 않은 바이트가 섞인 줄 | **1** | **0** | **0** | **0** |

이게 남 얘기가 아닌 이유: **nginx access/error 로그에는 스캐너가 보낸 그런 바이트가
실제로 섞이고, 우리는 그 로그를 grep 해서 "쿼리스트링이 샜는가"를 판정한다.**
게다가 크론은 보통 로케일이 비어 있고(POSIX) 사람은 UTF-8 셸에서 손으로 돌린다 →
로케일을 안 정하면 *"손으로 돌렸을 땐 괜찮았는데 크론에서만 다른"* 검사가 된다.
**보안 판정이 환경에 따라 달라지는 것**, 그게 고친 이유다.

**고친 방식**: `monitor.sh`·`monitor-selftest.sh` 에 `export LC_ALL=C`(전 검사가 바이트 단위 ·
인증서 만료일 `date -d "Aug 30 ..."` 파싱도 같이 안전해진다). `scrub()` 와 `job-run.sh` 의
사유 줄 추출은 **명령 단위**로만 붙였다 — `export` 하면 job-run.sh 가 돌리는 **파이썬 배치의
출력 인코딩까지** 바꾸게 된다(그쪽은 한글을 찍는다).
`market-index.sh` 는 `.*` grep 이 없고 `[[ "$REF" < "$EXPECTED" ]]` 도 세 로케일에서 같다 → 손대지 않았다.

### 8-4. 아직 안 고친 것 (다음 라운드)
`CR40-5`(배치가 아예 안 도는 상태의 별도 경보 · `last_success_at` 노후 감시) ·
`CR40-8`(전송 실패해도 `.active` 를 지우는 `clear_alert`) · `CR40-10`② ·
`SR36-5`(발송 상한) — ⛔ **이제 구현됐다(2026-08-03)**: `monitor-lib.sh` 의 `_send_quota_check`/`_send_quota_commit`, 기본 **하루 60통**(`RE_MON_SEND_MAX_DAY`). 세 가지를 못 박았다 —
① **상한에 닿는 순간 한 통은 나간다**(침묵과 억제를 구분 못 하면 그것도 실패다) ·
② **성공한 전송만 소진한다**(자격증명 없음·curl 실패로 0통 보내고 상한을 다 쓰면, 채널이 복구된 뒤 자정까지 전면 침묵한다 — CR47-1 이 그 결함이었다) ·
③ **상태가 깨지면 막지 않는다**(fail-open). 감시에서 의심스러울 때 조용해지는 쪽을 고르면 고장과 무사고가 같아 보인다. 상한값이 비숫자·0 이면 기본값으로 되돌린다.
> ⚠️ 2026-08-02 정리(CR44-8): `SR36-3` 잔여(따옴표 낀 키)와 `SR36-4`(`log()` 세탁)는 `CR-044` 라운드에 **닫혔다**(같은 문서 §2 가 그렇게 적는데 이 목록만 안 맞춰져 있었다). 위 셋은 여전히 열려 있다.

### 8-5. `CR-041` / `SR-037` 라운드에서 고친 것 (2026-08-01)

* **`CR41-2` 배치의 자기 단언이 한 달 헐거웠다 — 조용한 실패가 rc=0 으로 통과했다.**
  `market-index.sh` 가 기대 기준월을 **지금 시각**으로 계산했는데, 배치가 도는 시각은
  **1일 04:10** 이라 유예값(`MARKET_BATCH_READY=06:00`) 분기에 걸려 기대월이 **지난달로
  롤백**됐다. 실측 **30개월 중 28개월**에서 기대월이 자기 REF 보다 한 달 뒤처졌다.
  그 상태에서는 *"돌긴 돌았는데 완결월이 안 올라간"* 실패가 **성공으로 기록**되고,
  5시간 뒤 감시가 내보내는 *"매월 1일 배치가 안 돌았거나 실패했다"* 도 그때는 **거짓**이다.
  → 기준 시각을 **그 달의 배치 주기 시각**으로 고정(`batch_expected_ym`).
  30개월 × (1일 04:10) 불일치 **0**, 월중·말일 수동 실행 70회 거짓실패 **0**.
  자체검사에 **T3b** 신설 — *"1일 04:10 의 EXPECTED == 그 배치가 만드는 REF"* 를 18개월로 본다.
  **비교문이 있는지만 보던 T3 이 이걸 놓쳤다.** 값을 안 보는 검사는 검사가 아니다.
> ⚠️ **CR41-2 를 고치면서 드러난 것 — 다음 달에 이 경보가 울 수 있다(서버 실측).**
> 배치의 단언이 감시와 같은 기준이 되면서 **한 달 느슨하던 여유가 사라졌다.** 서버에서
> `market_price_index` 를 읽어 보니 **`scope='sido'` 의 완결 개수가 0인 달이 실재한다** —
> `2025-07`·`2025-08` 은 서울·경기·인천 **세 곳 전부 미완결**이다(계절적 거래량 감소를
> `_complete_flags` 의 건수 검사가 수집 누락과 구분하지 못한다 — 그 함수 주석이 스스로 적어 둔 한계).
> 그런 달이 끼면 `REF` 가 안 올라가고 **배치가 실패로 끝난다.** 최근 14개월 중 **2개월**이 그렇다.
>
> **⛔ 그때 적은 근거 ③이 20배 틀렸다 (CR42-1 · SR38-8). 그대로 남긴 뒤 고친다.**
> 옛 문장은 *"월 1회 배치라 최악이어도 **연 2~3통**"* 이었다. 그런데 같은 문단의 근거 ②가
> *"감시(`--daily` #11)는 **이미 같은 비교**를 하고 있다"* 였다 — 둘을 같은 기준으로 맞췄으니
> **그 달에는 감시도 매일 운다.** 감시 쿨다운은 86400 이고 `--daily` 는 하루 한 번 도니
> 정확히 **하루 한 통**이다. 근거 ②가 곧 피해의 원인인데 그 방향으로는 안 재 봤다.
>
> 실측 전제(`scope='sido'` 완결 0 = `2025-07`·`2025-08`·`2026-07`)로 **16개월 전수**를
> 자체검사(T10)가 센 값:
>
> | | 배치 | 일일 감시 | 해소 | 합계 |
> |---|---:|---:|---:|---:|
> | **전** | 3통 | **91통** | 1통 | **95통** |
> | **후** | 3통(경고) | **0통** | 1통 | **4통** |
>
> CR-040 이 차단한 옛 오탐이 연 35일이었다. 이건 그보다 크다. 그리고 **그 91통은
> 손으로 할 수 있는 것이 하나도 없는 알림**이다(근본 원인은 `_complete_flags` — 범위 밖).
> 대응 불가능한 경보가 한 달 연속 오면 사람은 그 키를 끈다 — 그리고 그 키에는
> **016 컬럼 누락과 시장지수 0행**이 함께 매달려 있었다. 진짜 신호가 같이 묻힌다.
>
> **그래서 고친 것 — 느슨하게 만든 것이 아니라 이름을 바로 붙였다.**
> ① **배치가 등급을 나눈다.** `DIAG` 는 이미 두 원인을 구분해서 계산하고 있었다:
>    `행없음`(원본 미유입 = 진짜 사건)은 **rc=1 그대로**, `…(미완결)`(표본 부족)은
>    **rc=0 + `경고:` 줄**. 설명되지 않는 모양(`완결` 행이 있는데 REF 가 작다 · 조회실패)은
>    **보수적으로 rc=1**. 모르는 것을 경고로 내리지 않는다.
> ② **`job-run.sh` 가 `경고:` 를 별도 키(`warn_<이름>`)로 보낸다.** 쿨다운 20일 →
>    월 1회 배치라 한 달에 한 통, 연속된 두 달은 각각, 같은 달 수동 재실행은 억제.
>    `last_rc=0` 이라 **`last_success_at` 이 한 달 굳던 것도 풀린다.**
> ③ **감시는 배치가 이미 말한 달에 침묵한다**(#11 · 위 표).
>
> **숨긴 것이 아니다.** 사용자가 받는 문장이 *"배치 실패"* 에서
> *"배치는 성공했다 · 기준월이 표본 부족으로 안 올라갔다 · 지금 할 조치는 없다"* 로 바뀐 것이고,
> 그게 **사실**이다(배치는 51초에 전 행을 적재했다). 도메인 코드 자신이 그렇게 적어 뒀다 —
> `timeadjust.py` `_complete_flags` docstring: *"보수적인 쪽의 오류이고, 기준월은 어차피
> '가장 최근'을 고르므로 그 달들이 기준월이 될 일은 없다"*. 옛 단언은 **그 문장과 정면으로
> 어긋났다** — 단언이 바로 그 달을 기준월로 삼아 실패시켰다.
>
> **진짜 미실행은 여전히 잡힌다**(T10 이 같이 센다): 배치가 아예 안 도는 시나리오는
> 183일 → **27통**(쿨다운 7일)이고, 그 동안 `marketstale.active` 가 남아 일일 요약
> 머리말에 매일 표시된다. 상태 파일이 없거나·지난달이거나·값이 깨졌으면 전부
> "안 돌았다"로 본다(fail-closed).
>
> **근본 해결은 백엔드 쪽**(`_complete_flags` 의 건수 검사가 계절성을 흡수하도록)이고
> 그건 이 범위(`deploy/**`)가 아니다 → 다음 라운드 과제.
> **다음 배치(2026-09-01)가 이 사유로 걸린다** — `2026-07` 은 이미 0/3 이고
> 최소표본 1,422 < 문턱 ≈2,080 이라 신고지연 30일이 다 지나도 못 넘는다.
> 그날 사용자에게 가는 것은 **"실패" 한 통이 아니라 "성공 + 확인할 것" 한 통**이다.

* **`SR37-1`/`CR41-3` T2 트립와이어가 세 방향으로 눈이 멀었다.**
  `size < off` 를 무조건 로테이션으로 해석해서, ⓑ`.1.gz` 만 남는 압축 설정 ⓒ침입자의
  `: > auth.log` ⓓ회전 뒤 새 파일이 옛 오프셋을 넘게 자란 경우를 **전부 0건으로 넘겼다**.
  → **inode + 크기**로 판정을 셋으로 나눴다(회전 / 축소 / 증가). 축소가 회전으로 설명되지
  않으면 `authshrink` 경보 + 감시불능 사유에 남긴다. 압축 회전본은 풀어서 센다.
  격리 재현 결과 **고치기 전 ⓑⓒⓓ 침묵 → 고친 뒤 전부 잡힘**, 대조군(정상 증가·침입 없는
  평범한 로테이션)은 **여전히 조용**하다.
* **`CR41-3` 자체검사가 "한 대상만 잃은" 상태를 안 봤다.** 그래서 SSH `blind_add`
  한 줄을 지워도 49/49 통과했다. → T4 에 **묶음별 단독 소실**(auth.log 만 · error.log 만 ·
  nginx 묶음만) + **전부 정상일 때 안 우는지**까지 넣었다.
* **`CR41-1`** 리뷰 로그의 예시 두 줄이 저장소 비밀검사에 걸려 **관문이 붉었다** →
  `<…형태>` 로 마스킹(검사는 그대로). 검증 벡터의 정본은 `monitor-selftest.sh` T7 이다.
* **`CR41-4`** `scrub` 금액 규칙에 ③(토큰 인접) 추가 + §2 의 근거 문장을 사실로 정정.
* **`CR41-5`** 로케일 진단 문장 정정(§8-3b). 조치·범위는 그대로 둔다.
* **`CR41-7`** `sshpw_off` 를 **읽은 구간과 정확히** 맞춘다(같은 줄 중복 계수 제거).
* **`CR41-8`/`SR37-4`** `kv/*`·`alerts/*` 를 0600 으로, 자체검사 `trap` 에 `INT TERM HUP`.
* **`CR41-6` 자체검사가 간헐적으로 거짓 FAIL 을 냈다 — 원인을 붙잡았다.**
  실측으로 조건을 갈랐다: **순차 실행은 8회·30회·8회 전부 정상**, **동시 실행에서만** 붉었다
  (4개 동시 24회 중 1회 · 8개 동시에서 재현). 감시 본체는 결백하다 — 같은 시나리오를
  직접 **200회** 돌려 출력 결손 **0**을 확인했다. 붉은 것은 **언제나** `printf '%s' "$OUT" | grep -q …`
  처럼 **파이프라인을 `if` 조건에 쓴 검사**였고, 같은 시나리오의 **파일 grep 검사는 한 번도** 붉지 않았다.
  → 이 파일은 `set -o pipefail` 아래서 돈다. 파이프라인 상태는 *"안 맞았다"* 말고도
  **읽는 쪽(`grep -q`)이 먼저 끝나 쓰는 쪽이 SIGPIPE 로 죽거나, 부하로 fork 가 실패**해 0이 아닐 수 있다.
  그러면 **없는 결함을 보고**하고, 부정형(`if … then ng`)에서는 반대로 **있는 결함을 놓친다** — 이쪽이 더 나쁘다.
  → **조건 자리의 파이프라인을 전부 없앴다**(셸 내장 `case` 기반 `has()` · 파일은 `grep -q 패턴 파일` 한 프로세스).
  겸해서 *"감시가 아무 출력도 안 냈다"* 를 **검사 실패와 구분해서** 말하게 했다.
  고친 뒤 **8개 동시 × 32회 전부 그린**(고치기 전 같은 조건에서 재현되던 것).
* **덤으로 찾은 것 — `inode` 는 "같은 파일"의 증거가 못 된다.**
  회전/축소를 `inode` 로 가르는 1차 수정이 **(b) `.1.gz` 시나리오에서 실패**했다. 원인은
  **inode 재사용**이다(실측: 파일을 지우고 새로 만들면 커널이 같은 번호를 준다).
  `compress`(delaycompress 없음) 로테이션은 `.1` 을 지우므로 이게 실제로 일어난다.
  → **"회전본이 우리가 마지막으로 본 뒤에 생겼는가"(mtime)** 를 증거로 함께 본다.
  침입자의 `truncate` 는 회전본을 새로 만들지 않으므로 이 증거가 없다.
  그래서 **(c2) 옛 `.1` 이 오프셋보다 큰 채로 남아 있는 상태의 truncate** 도 갈린다 —
  자체검사에 그 케이스를 넣었고, 이 판정을 되돌리는 변이 2종(`inode` 만 보기 / `mtime` 무시)이 각각 죽는다.

**변이 12종 전부 죽는다**(**`CR-041` 시점 기준** — 서버 실측 · 자체검사 83건 당시. 그 뒤로 종수·검사 수가 늘었다. 최신 값은 §9 를 볼 것 — CR43-5):
배치 기대월 인자(2종) · SSH `blind_add` 한 줄 · 로테이션 판정(크기만) · `inode` 만 보기 ·
`mtime` 무시 · `scrub` 금액토큰 규칙 · `.1.gz` 분기 · `MARKET_BATCH_READY` 드리프트 ·
`authshrink` 경보 삭제 · 오프셋 절단 삭제 · `blind` 항상 켜기(대조군이 무언가를 붙잡는지).

---

### 8-6. `CR-043` / `SR-039` 라운드에서 고친 것 (2026-08-01)

**공통점부터 적는다 — 이번 것 둘은 전부 "관문이 한쪽만 봤다" 이다.**
`CR43-1` 은 `--daily` 만 돌려서 `--fast` 의 되돌림을 못 봤고, `CR43-2` 는
*"울면 안 되는 쪽"* 만 상수로 단언해서 *"울어야 하는 쪽"* 을 못 봤다.
그래서 이번 라운드의 규칙을 하나로 적는다:
**억제·해소를 만드는 모든 코드는, 그 반대 방향(울어야 하는 경우 · 다른 모드)에서도
한 번 돌려 본다.** 지금 그 대칭을 갖춘 것: `logperm`·`logleak`·`cert`·`warn_*`·
**`logblind`(모드 대칭 · 신규)**·**`marketstale`(방향 대칭 · 신규)**·
**`daily_dead`/`fast_dead`(상호 감시 대칭 · 신규)**.

* **`CR43-1` `--fast` 가 `--daily` 전용 사유를 매일 지웠다 (차단이었다 · 이 라운드가 만든 결함).**
  `check_cert` 는 `raise_alert` 를 안 하고 `blind_add` 만 하도록 설계됐다(CR42-3).
  그러면 *"인증서를 못 봤다"* 가 사람에게 닿는 길은 `logblind` **하나뿐**이다.
  그런데 5분 뒤 `--fast` 가 **인증서를 돌리지도 않은 채** 자기 `BLIND` 가 비었다는 이유로
  그 경보를 지우고 *"해소: 로그 감시 대상 정상 (권한·유출·5xx·SSH 검사 전부 수행)"* 을
  보냈다. 다음 일일 점검이 다시 켠다 → **raise 2 / clear 1 이 매일 반복**되고
  쿨다운 21600 도 `.sent` 가 지워져 무력화된다. CR-040 이 차단하고 CR42-3 이 다시 차단한
  문장이 **세 번째로** 돌아온 것이고, 이번에는 **고치는 과정에서** 들어왔다.
  → daily 전용 사유를 `BLIND_DAILY` 로 따로 모아 `kv/blind_daily` 에 남기고,
  `--fast` 는 그 키가 살아 있으면 **clear 하지 않는다**(요약에는 "사유가 남아 있다"를 적는다).
  해소 문구도 모드에 맞췄다. 자체검사 T5b 가 **모드 대칭 + 되돌아오는 길**을 함께 본다.
* **`CR43-2` 이번 라운드의 대표 성과가 검증되지 않았다 (차단이었다).**
  T10 의 *"후 = 0통"* 이 **계산이 아니라 상수**였다. 그래서 억제의 **극성**과 **쿨다운**이
  관문에서 자유로웠고, 리뷰어가 심은 변이 2종이 **139/0/2/HARN 0 · rc=0** 으로 생존했다.
  게다가 그 코드는 `docker`+`psql` 이 있어야 도달해 **한 번도 실행된 적이 없었다.**
  → 판정을 `check_market_stale(ref, expected)` 로 떼어 내고, 자체검사가 그것을
  **코드에서 뽑아** `_market_batch_ran_this_month`·`raise_alert` 를 가짜로 물린 뒤
  **하루씩 돌려서** 통수를 센다. 미실행 시나리오에는 **상한**(밀린 날 10일당 최소 1통)과
  **쿨다운 일치** 단언을 걸었다. 격리 사본 실측:

  | | 통과 | 실패 | HARN | rc | 무엇이 드러났나 |
  |---|---:|---:|---:|---:|---|
  | 대조군(무변이) | 167 | **0** | 0 | 0 | — |
  | **M1** 극성 반전 | 162 | **5** | 0 | 1 | 배치가 돈 달 0→**14통** · **미실행 183일 27→0통(완전 침묵)** |
  | **M2** 쿨다운 1년 | 166 | **1** | 0 | 1 | 미실행 183일 27→**1통** (최소 기대 18통) |

* **`CR43-3` 관문이 부하에서 결정적이지 않았고, HARN 이 그것을 못 잡았다.**
  `want()/avoid()/live()` 는 **로그 파일을 읽는 검사만** 덮는데, 포크를 가장 많이 쓰는 것은
  **날짜 루프**다. 거기서 `date` 가 죽으면 값이 비고 `[[ "$a" < "$b" ]]` 는 **무조건 참**이 된다
  — `monitor.sh` 가 스스로 금지한 형태다. → `nz()` 가드 + `harn_if()`.
  **실측으로 확인했다**: 무변이 사본을 3중 동시 실행하니
  `fork: retry: Resource temporarily unavailable` 이 실제로 났고, 결과는
  **통과 167 · 실패 0 · 하네스오류 1 · rc=1** 이었다 —
  예전 같으면 *"월중 수동 실행에서 기대값이 DB 를 앞선다"* 라는 **없는 결함**으로 보고됐을
  자리다(리뷰어 실측 138/1/2/HARN 0 이 정확히 그것이다). 이제 검사 결과(167/0/2)는
  그대로이고 부하는 HARN 으로 분리된다.
* **`SR39-1` 메타데이터로는 원리적으로 못 보는 칸 — 출처를 하나 더 만들었다.**
  ①`authfake`(mtime 과 크기의 모순) ②**journald 교차**. 격리 재현으로 확인:
  침입 줄을 `dd conv=notrunc` 로 **같은 길이로 덮어써** 크기·inode 가 **둘 다 보존**된
  상태에서 `authfake` 가 뜬다. **한계도 적는다** — journald 도 root 면 지운다.
  이 교차는 침입을 불가능하게 만들지 않고 **두 곳을 다 지워야 하게** 비용을 올릴 뿐이고,
  그 비용을 치렀다는 사실은 `sshjournal` 이 알린다. 그래서 이 검사에 대한 정직한 문장은
  여전히 *"정리하지 않는 침입자의 비밀번호 로그인 성공을 탐지한다"* 이고,
  그 이상은 **문을 닫는 것**(`PasswordAuthentication no`)이 답이다.
* **`SR39-2` 신선도 증거가 mtime 하나였다** → 크기 증가와 **함께** 본다(`authfake`).
  같은 라운드가 회전 판정에서 *"mtime 은 단독으로 쓰지 않는다"* 고 못 박아 놓고
  ⓪ 블록에는 그 원칙을 안 썼던 자리다.
* **`SR39-3` 평문 `.1` 이 `.1.gz` 로 바뀌는 상태**는 이 서버(delaycompress)에 없다 →
  `authedit`(흔적 삭제로 읽히는 이름)으로 올렸다. 오탐 0의 근거는 조건이
  **설정이 아니라 관측**이라는 것이다(대조군이 자체검사에 있다).
* **`SR39-4`/`SR39-6` 비밀 규칙** — `scrub` 에 이름 없는 형태(`sk-ant-`·`KakaoAK`·`Basic`·맨 JWT)와
  `pwd=`/`credential=` 추가. 저장소 위생 관문에 **대문자 `*_KEY` 계열**과 접두 규칙 추가
  (실측: 값 12자 기준 오탐 2건 — 둘 다 원장의 설명용 가짜값 · **16자 기준 0건** →
  16자로 잡았다. 이 프로젝트가 담을 키는 전부 32자 이상이다).
  관문 **자신**에 대한 변이 시험 2개도 같이 넣었다(잡아야 하는 6형태 · 막으면 안 되는 7형태).
* **`SR39-5` 실제 전송 줄**(`curl --data-urlencode "text=${text}"`)을 구조로 못 박았다 —
  `DRY_RUN` 분기만 지나가는 시험은 그 한 겹 안쪽을 못 본다.
* **`CR43-4` 인증서 만료일 파싱 실패가 거대 음수로 흘렀다** →
  `[ -z "$end" ]` 와 **같은 처리**(못 읽음)로 보낸다.
* **15번째 자리(스스로 찾음) — 상호 감시의 대칭이 같은 함수 안에서 깨져 있었다.**
  `--daily` 는 *"5분 감시가 한 번도 돈 기록이 없다"* 를 경보로 만드는데, `--fast` 는
  같은 상황에서 조용히 `return 0` 했다(실측: `--fast` 8회에 `daily_dead` **0건**).
  `--daily` 크론 한 줄이 빠지면 **일곱 검사**가 통째로 없는 채로 아무 신호가 없다.
  → 첫 fast 실행 시각을 기억하고 30시간 유예 뒤 `daily_dead`.
  ⚠️ **근거 정정(CR44-4)**: 예전 문장은 *"지금 배포가 정확히 크론 2줄을 새로 넣는 일"* 이라고 적었는데 **아무도 안 재 본 문장**이었다. 크론은 2026-07-30 에 이미 들어갔다(`CR-041` 우리 3줄 · `SR-038` realestate 3줄 · `DEPLOY.md §9-1` 에 crontab 단계 자체가 없다). 결함과 조치는 그대로 유효하고, 위험은 **크론 한 줄이 사라지거나 다음에 다시 설치할 때** 열린다.
* **문서가 '남은 구멍(다음 라운드)' 으로 두 라운드 미뤄 둔 것**(`log()` 세탁)도
  **피해를 재현해서** 닫았다(§2). 미룬 것 자체가 문제였다 — 아무도 그 경로를 안 재 봤다.

### 8-6. `CR-044` / `SR-040` 라운드에서 고친 것 (2026-08-02) — **서버에서 붉은 것을 고쳤다**

`199d9fe` 로 서버 반영이 끝난 **뒤**, 서버에서 자체검사를 돌리니 `168/2/0 · rc=1` 이었다.
윈도우에서는 같은 코드가 `167/0/2` 로 초록이었다. **그 차이가 이 라운드의 전부다.**

* **⛔ `SR40-1` — 두 번째 출처가 "살아 있는데 아무것도 안 보는" 상태를 아무도 못 봤다.**
  🔴 **SR41-1 로 정정됨(2026-08-03)** — 아래 서술은 `--vacuum-time` 을 잡는다는 뜻이 아니다.
  1회성 삭제는 저널이 몇 초 만에 다시 차서 **탐지 확률 약 4.4%**(공격자가 접속 1회를 만들면 0%)다.
  이 검사가 잡는 것은 **지속되는 눈멂**뿐이다. 자세한 것은 §7b-④ 표.
  `journalctl --rotate --vacuum-time=1s` 를 하면 `journalctl` 은 **그대로 응답하고**
  ssh 조회만 0줄이 된다. 옛 코드는 그 상태에서 완전히 침묵했고, 요약은
  `journald 같은 구간 0건 (기대 0/0)` 이라며 **적극적으로 무사고를 선언**했다.
  같은 상태를 만드는 길이 더 있다: `Storage=none` · ssh 유닛 이름 변경/소켓 활성화 ·
  저널 손상. **가장 날카로운 증거는 자체검사 안에 있었다** — *"두 출처가 다 0 이면 침묵
  (오탐 0)"* 대조군이 곧 그 공격 성공 상태였다.
  → **창 안의 ssh 유닛 메시지 총량(jtot)** 을 함께 세고, 그것이 0 인데 같은 창의
  auth.log 가 sshd 줄을 받았으면 **모순**으로 보고 `sshjournal` 을 올린다.
  **오탐 0 의 근거(서버 실측)**: 24시간 ssh 유닛 29,195줄 → 5분 버킷 288개 중 0줄 버킷
  **0개**(최소 21 · 평균 101). 1분 버킷은 1,440개 중 30개가 0줄이라 **창 ≥120초** 를 요구하고,
  auth.log 는 sshd 말고도 자라므로(CRON·systemd-logind·systemd-user) **같은 창의 sshd 줄 ≥2**
  를 함께 요구한다. 실제 5분 창 3회 · 격리 DRY-RUN 으로 재서 **경보 0건**을 확인했다.
* **`SR40-3` `authfake` 의 사정거리를 사실대로 적었다.** 조건이 `크기 == 직전 오프셋` 이라
  **로그가 자라는 동안에는 성립하지 않는다** — 그리고 이 서버는 항상 자란다.
  즉 운영 조건의 x9 를 잡는 것은 journald 교차 하나뿐이다. 그 문장을 코드 주석·이 문서·
  그리고 **검사**(`x9b`: 자라는 창에서 `authfake` 가 안 뜬다)로 함께 못 박았다.
* **`CR44-1` `sshjournal` 에 해소 경로가 없었다.** 형제 `authfresh` 는 꺼지는데 이쪽만
  안 꺼져서, journald 가 한 번 흔들리면 일일 요약 머리말이 영영 `미해소` 였다.
  → **교차가 실제로 돌아왔을 때만**(응답 O + 그 창의 ssh 메시지 >0) 끈다.
  응답만 하고 0줄인 상태에서 끄면 그것이 `CR40-2` 가 막은 거짓 해소다 — 그 대조군도 넣었다.
* **`CR44-9` `api5xx` 도 형제 중 유일하게 clear 경로가 없었다** → 델타 0 인 실행에서 끈다.
  (못 읽은 경우에는 위에서 `blind_add` 하고 돌아가므로 여기 도달하지 않는다 — 대조군 있음)
* **`CR44-2` 가 지목한 "clear 를 부를 자격" 을 전수로 훑었다.**
  관문이 `ALERT-CLEARED` 를 **행동으로** 보는 키가 6개 → **12개**가 됐다:
  기존 `logperm`·`logleak`·`logfresh`·`cert`·`logblind`·`warn_*` 에
  **`marketstale`(M3) · `sshjournal` · `api5xx` · `authfresh` · `authshrink` · `daily_dead`/`fast_dead`**
  를 더했다. 남은 것은 `dbstruct`·`dbstruct_cfg` 뿐이고, 그 둘은 `docker`+`psql` 이 있어야
  도달하므로 **오늘 관문이 못 본다**(문자열 검사만 있다) — 숨기지 않고 여기 적는다.
* **`CR44-3` 하네스 가드가 절반이었다** → T10 두 루프에 `nz`/`harn_if`, 픽스처 바이너리
  동작 확인. 변이로 각각 **HARN 1 · 실패 0** 을 확인했다.
* **`CR44-6` 배포 절차가 순서를 지킬 수 없게 쓰여 있었다.** `DEPLOY.md §9-1` 은
  `install`(제자리 덮어쓰기) **뒤에** 자체검사를 돌리게 시켰다. 그래서 붉었을 때는 이미
  크론이 새 코드로 돌고 있었다. → **스테이지 → 거기서 검사 → 통과했을 때만 `mv -f`**
  로 바꾸고, 리눅스 기대 숫자를 실측값으로 적었다(197/0/0 · rc=0).
* **`CR44-4`/`CR44-8`/`CR44-10` — 안 잰 문장과 fail-open 정리.** *"지금 배포가 정확히
  크론 2줄을 새로 넣는 일"* 은 사실이 아니었다(크론은 2026-07-30 에 들어갔다).
  §8-4 목록에서 이미 닫힌 둘을 뺐다. `check_peer_alive` 의 kv 산술에 숫자 가드를 붙였다.

**그리고 이 라운드가 배운 것 하나** — *"관문의 초록이 환경 덕일 수 있다."*
`(x9)` 는 **초 경계를 우연히 넘어서** 초록이었고, `(교차)` 는 **진짜 journalctl 이 없어서**
초록이었다. 둘 다 이제 환경과 무관하게 결정적이다. 그래서 배포 절차의 판정 기준도
**"윈도우에서 초록"** 이 아니라 **"서버에서 rc=0"** 으로 바뀌었다.

---

---

## 9. 감시의 감시 — `deploy/monitor-selftest.sh`

셸 4종(약 600줄)은 **root 로 5분마다** 도는데 자동 검사가 하나도 없었다(CR40-3).
`monitor-selftest.sh` 는 **이번에 고친 자리**를 붙잡는다. 전부 덮지 않는다.

```
T1   bash -n 5파일
T2   기대월 — 365일을 하루씩 · 오탐 0 / 옛 공식 35일 오탐 / 매월 1일 경계 5시각
T3   기대월 공식이 배치·감시 두 곳에서 같은가(본문 + MARKET_BATCH_READY) + 단언이 있는가
T3b  **값까지** 본다 — 1일 04:10 의 EXPECTED == 그 배치가 만드는 REF (18개월) ·
     기준 시각을 안 고정하면 몇 개월이 어긋나는지 · 월중 수동 실행이 안 죽는지  (CR41-2)
T4   대상 0개 → 경보 · clear 금지 · 켜진 경보 보존 · '해소' 0통 · 5xx 기준값 보존
     + **묶음별 단독 소실**(auth.log 만 / error.log 만 / nginx 묶음만) + 정상일 때 무경보 (CR41-3)
T5   대조군 — 정상이면 안 울고, 0644 는 잡고, 해소는 실제로 나가고(logleak·**logblind**), mtime 은 구분한다
T5b  **인증서 fail-open** (CR42-3) — 대상 0개일 때 clear 금지 · 켜진 경보 보존 · blind 에 실림 ·
     '9999일' 이 어디에도 안 나옴 + **양성 대조군**(정상 인증서 1장이면 실제로 해소 · 임계 미만이면 실제로 경보)
     + **모드 대칭**(CR43-1) — daily 로 `logblind` 를 띄운 **그 상태에 `--fast` 를 이어 돌린다**:
       clear 0건 · `.active` 생존 · 해소 0통 · 요약이 "사유가 남아 있다"고 적는가 ·
       그리고 **되돌아오는 길**(인증서가 정상이면 daily 가 실제로 해소하고 kv 기록도 갱신한다)
T6   SSH — 실패·공개키는 안 세고, 비밀번호 성공만 세고, 본문에 원문이 없다
     + 회피 **9케이스**: 정상회전 / .1.gz / `: >` / 옛 .1 이 큰 채로 truncate / 오프셋 초과 성장 /
       **rm+재생성(h)** / **서버형 delaycompress 에서 rm+재생성(l)** / **`: >`+`touch .1`(x1)** /
       **회전본 편집(x4)** / **로그 동결(x6)**
     + 대조군 4종(정상 증가 · 평범한 로테이션 · **서버 설정 그대로의 주간 회전** · 동결 해제)
     + 오프셋 정합 · 파일시스템 능력 확인(rename 이 inode 를 보존하는가)
T6d  **fail2ban** (2026-08-04 · 2026-08-05 보강) — ①데몬 죽음(2회 연속 · 1회차는 침묵)
       ②**서비스는 active 인데 규칙만 빠짐** ③필터 정체(24시간)
       ④**점프는 있는데 그 앞의 ACCEPT 가 무력화**(SR45-7) — 무해한 앞줄 5종은 안 울고,
         하위 체인은 단정 대신 **감시불능**
     + **해소 3종**(f2b_dead·f2b_rule·f2b_stale 각각 `.active` 삭제까지) ·
       fail-open(`iptables` 실패를 '규칙 없음'으로 단정 안 함 · 켜진 경보 보존 · 거짓 해소 없음) ·
       없는 jail 은 **`rc=255`**(CR49-1 재측정 — 옛 기록 `rc=0` 은 거짓이었다. 그래도 rc 로
       판정하지 않는다) · **한 구간 500건 차단에도 경보 0통** ·
       카운터 리셋을 회복이라 말하지 않음 · 포트 판정(단일·목록·범위·무지정) ·
       **차단 IP 유출 가드**(SR45-4 — 픽스처가 실제로 뱉는 `9.9.9.9`·`8.8.8.8` 이 요약·로그·
       알림 어디에도 없어야 한다. `scrub()` 에는 IP 규칙이 없어 세탁으로는 안 걸린다) ·
       실행파일 탐색(**크론 PATH 에는 `/usr/sbin` 이 없다** · SR45-5 이후로는
       **PATH 앞의 가짜가 안 불려야** 통과 — 주입은 `RE_MON_BIN_DIR` 로만)
T6e  **IPv6 SSH 차단**(SR-046 · 2026-08-05) — ①**DROP 이 없다**(픽스처는 PM 조치 **이전의 실제
       상태** = fail2ban v6 점프만 있는 모양 → 점프로 판정하는 코드는 여기서 초록인 채로 거짓말한다)
       ②DROP 앞의 ACCEPT 로 무력 ③**출발지가 붙은 DROP·lo 전용·udp·f2b 점프를 우리 보호로 세지 않는다**
       ④유닛이 enabled 아님 ⑤**유닛 파일 삭제**(`is-enabled` 빈 출력을 감시불능으로 덮지 않는다)
       ⑥유닛 failed 면 규칙 경보를 보류해 **한 사고 한 통**
     + **해소 2종**(v6ssh_rule·v6ssh_unit 각각 `.active` 삭제까지) ·
       fail-open 3종(`ip6tables` **부재**(v6 미사용 호스트) · 실행 실패 · systemd 불가 —
       전부 단정도 거짓 해소도 안 하고 켜진 경보를 보존) ·
       대조군 2종(정상 · **흔한 v6 방화벽 앞줄**(lo·ESTABLISHED·443·`ipv6-icmp`)에 오탐 0) ·
       **v6 주소 유출 가드**(`2001:db8::7` 이 요약·로그·알림 어디에도 없고 `<ip>` 로 가려진다) ·
       판정표 11케이스를 **함수째 뽑아** 확인 · 두 모드가 실제로 `check_v6ssh` 를 부르는가
T7   scrub — 지울 것 21종 / 남길 것 6종 + **나가는 경로(`send_telegram`)가 실제로 scrub 을 타는가**
     + 문서와 코드가 같은 말인가(구멍까지 적었는가)
T8   monitoring.md 의 크론 3줄과 스크립트 사용법이 맞는가
T9   job-run.sh 가 배치 로그를 0640 으로 만드는가 · 기존 로그를 안 지우는가
T6b  **상호 감시의 대칭** (15번째 자리) — 일일 점검이 **한 번도** 안 돈 경우 · 설치 직후 오탐 없음 ·
     반대쪽(`fast_dead`)도 여전히 사는가
T10  **경보 볼륨** (CR42-1) — 완결 0인 달이 낀 16개월을 전수로 돌려 **통수를 센다**(전 95 / 후 4) ·
     **배치가 아예 안 돌면 여전히 우는가**(183일 → 27통) · 억제 판정 함수를 코드에서 뽑아 4케이스 ·
     기준월이 `dbstruct` 키에서 실제로 분리됐는가
     + **CR43-2** — "후" 를 상수로 적지 않는다. `check_market_stale()` 을 **코드에서 뽑아**
       `_market_batch_ran_this_month`·`raise_alert` 를 가짜로 물리고 **하루씩 돌려서** 센다.
       미실행 시나리오에는 **상한**(밀린 날 10일당 최소 1통)과 **쿨다운 일치** 단언을 건다.
       ⚠️ 이 구획은 `docker`·`psql` 없이 돈다 — **못 도는 검사는 없는 검사다**
T11  **배치 등급** (SR38-8) — 경고는 rc=0 · 별도 키 · `last_success_at` 갱신 · 본문에 '배치 실패' 없음 ·
     경고 해소 · **진짜 실패(rc≠0)는 예전 그대로**
```

- 안전: `RE_MON_DRY_RUN=1` + 임시 상태 디렉터리 + 닫힌 포트(127.0.0.1:9). **알림 0통**.
- 소요: **서버(리눅스) 수십 초** · 윈도우 체크아웃은 `date`·`bash` 프로세스 비용 때문에 **약 20분**
  (검사가 늘어 5~7분에서 늘었다. 서버에서는 여전히 수십 초다).
- 빨리 돌려 볼 때만 `SELFTEST_MONTHS=1`(기본 12 = 365일) · `SELFTEST_BATCH_MONTHS`(기본 18).
  **기본값을 줄이지 말 것** — 리뷰가 요구한 "한 해 오탐 0"이 그 기본값이다.
- 검사 **197건**(2026-08-02 · `CR-044`/`SR-040` 반영 후 · **서버 `/opt/realestate/scripts`
  실측 통과 218 · 실패 0 · 건너뜀 0 · 하네스오류 0 · rc=0** · 사본 실행은 217/0/1). 직전 라운드는 167건이었다.
  ⛔ **그 167 은 윈도우 숫자였고, 같은 코드가 서버에서는 `168/2/0 · rc=1` 이었다**(2026-08-02).
  붉은 둘은 `(x9)` 와 `(교차) journald 가 사라져도…` 이고 **둘 다 검사가 옳았다** —
  ①`(x9)` 는 기준값 실행과 `dd` 가 같은 초 안에 끝나면 성립하지 않는데 리눅스가 빨라서
  그렇게 됐다(윈도우는 느려서 우연히 통과했다) ②`(교차)` 는 "journald 가 사라진" 상태를
  **PATH 에서 가짜를 빼는 것**으로 만들었는데 서버에는 진짜 `journalctl` 이 있다.
  → 전자는 기준 mtime 을 과거로 못 박아 **결정적**으로 만들고 픽스처 실패는 HARN 으로,
  후자는 "사라짐"을 **응답 실패**(`-n 1` 이 실패하는 가짜)로 바꿔 두 환경에서 같게 만들었다.
  **판정 기준은 통과 건수가 아니라 `실패 0 · 하네스오류 0 · rc=0` 이다** —
  실행 위치에 따라 SKIP 이 정당하게 달라진다(임시 사본에서는 문서 검사 1건이 SKIP).
- **하네스 오류를 검사 실패와 분리해 센다**(SR38-9). 보안리뷰가 같은 파일을 2회 돌려
  80/1/2 · 78/3/2 로 갈렸는데, **실패 원인이 전부 하네스가 자기 임시파일을 못 만든 것**
  (공용 `%TEMP%` 96%)이었고 **검사 로직이 잡은 실패는 0건**이었다. 더 나쁜 것은
  **부정형 검사**다 — 파일이 없으면 조용히 `ok` 가 되어 **있는 결함을 놓친다**(CR41-6 이
  "이쪽이 더 나쁘다"고 한 방향). → 로그를 보는 검사는 전부 `want()`/`avoid()` 를 거치고,
  파일이 비었으면 `ok` 도 `ng` 도 아닌 **HARN** 으로 보고한다. 마지막 요약이
  *"통과 N · 실패 N · 건너뜀 N · **하네스오류 N**"* 을 따로 적고, HARN>0 이면 종료코드도 1 이다.
  **관문의 초록/빨강을 근거로 쓰려면 HARN 이 먼저 0 이어야 한다.**
- **⛔ 그 문장이 한동안 참이 아니었다 (CR43-3 · 이번에 고쳤다).** `want()/avoid()/live()` 는
  **로그 파일을 읽는 검사만** 덮는데, 포크를 가장 많이 쓰는 것은 **날짜 루프**(T2·T3b·T10)다.
  거기서 `date` 가 죽으면 값이 **빈 문자열**이 되고 `[[ "$ref" < "$exp" ]]` 는 **무조건 참**이 된다
  — `monitor.sh` 가 *"빈 문자열과 비교하면 무조건 참이라 헛운다"* 며 **스스로 금지한 형태**를
  관문이 쓰고 있었다. 실측: 같은 파일 3회 동시 실행에서 139/0/2 · 139/0/2 · **138/1/2**,
  붉은 것은 T3b, 원인은 `fork: retry: Resource temporarily unavailable`, 그런데 **HARN 은 0**.
  → 날짜 루프의 값에 `nz()` 가드를 걸고, 비면 `ok` 도 `ng` 도 아닌 **HARN** 으로 센다
  (루프가 끝난 뒤 `harn_if` 로 한 번만 보고한다).
  ⚠️ **그 조치가 처음에는 절반만이었다(CR44-3).** `nz`/`harn_if` 가 T2·T3b 에만 들어가고
  같은 라운드가 새로 쓴 **T10 에는 없었다**. 실측 두 방향: `FROZEN_REF` 가 한 번 비면
  **실패 3 · HARN 0**(없는 결함 보고) · 스테일 달의 `dexp` 하나가 비면 볼륨 모형이
  91→61통으로 조용히 약해지며 **전건 통과 · HARN 0**(있는 결함 놓침 — 더 나쁜 쪽).
  2026-08-02 에 T10 두 루프에 가드를 넣고 변이로 확인했다(각각 **HARN 1 · 실패 0**).
  함께: 가짜 `journalctl` 같은 **픽스처 바이너리가 기대대로 도는지 먼저 확인**하고,
  아니면 검사 실패가 아니라 HARN 으로 보고한다.
  ⚠️ 그러므로 *"HARN 0 이면 초록을 근거로 쓸 수 있다"* 가 지금 참인 범위는
  **T2 · T3b · T10 과 ㉯(픽스처 확인)** 이다. 나머지 구획은 아직 그 보증이 없다.
- **CR-043 라운드에서 관문에 더한 것 — "무엇을 죽이는가" 로만 평가한다.**
  리뷰어가 심어 **생존했던** 변이 2종이 이제 각각 죽는다:
  **M1** `check_market_stale` 의 `if _market_batch_ran_this_month` **극성 반전**
  (→ 배치가 아예 안 돈 달에 **완전 침묵**. 크론이 사라져도 아무 신호가 없다) ·
  **M2** `raise_alert marketstale` 쿨다운 `604800`→`31536000`(1년)
  (→ 진짜 미실행 183일에 27통이 **1통**. 옛 관문은 `발송 1통` 을 **출력하면서** PASS 라고 적었다).
  둘 다 이유는 하나였다 — **"후 = 0통" 이 계산이 아니라 상수**였고, 그 코드는
  `docker`+`psql` 이 필요해 **한 번도 실행된 적이 없었다.**
- 변이 검사(고친 자리를 되돌리면 죽는가) **20종 전부 실패로 잡힌다** — 배치 기대월 인자 2종 ·
  SSH `blind_add` 한 줄 · 로테이션 판정(크기만) · `inode` 만 보기 · 회전본 `mtime` 무시 ·
  `mtime` 비교 뒤집기 · `scrub` 금액토큰 규칙 · `.1.gz` 분기 · `MARKET_BATCH_READY` 드리프트 ·
  `authshrink` 경보 삭제 · 축소 `blind_add` 삭제 · 오프셋 절단 삭제 · `blind` 항상 켜기 ·
  **`clear_alert logblind` 삭제**(스스로 찾은 8번째 자리 — 켜지기만 하고 안 꺼지는 경보는
  곧 무시되는 경보다. T4 는 켜지는 것만, T5 는 logleak 만 보고 있었다) ·
  **회전 강한증거(`.1` inode == 직전 inode) 삭제**(→ h·l 이 죽는다) ·
  **`replaced` 분기 삭제** · **`authfresh` 삭제**(→ x6) · **`authedit` 정체검사 삭제**(→ x4) ·
  **`check_cert` 의 대상 0개 가드 삭제**(→ T5b) ·
  **`send_telegram` 의 `| scrub` 삭제** — ⛔ **스스로 찾은 10번째 자리**.
  T7 의 모든 검사가 `scrub()` 을 **직접** 부르고 있어서, 알림이 실제로 나가는 길에서
  세탁을 통째로 빼도 **자체검사 전체가 초록이었다**(직접 확인했다). 비밀 세탁의
  마지막 관문이 무방비였던 것이다. 지금은 T7 이 `send_telegram` 의 DRY-RUN 출력을 본다.
- **`fail2ban` 라운드(2026-08-04) 실측** — 서버 격리 사본(`/root/rev-f2b`)에서
  **통과 271 · 실패 0 · 건너뜀 1 · 하네스오류 0 · rc=0**(직전 231 · 소요 2분 5초).
  ⚠️ **지금 실측은 271 이 아니다** — SR-045 조치(`_f2b_order_scan` 신설 · IP 가림 · 하네스
  관문 · T6d 픽스처 관문)로 검사가 늘어 **2026-08-05 기준 통과 290 · 실패 0 · 건너뜀 1 ·
  하네스오류 0 · rc=0**(소요 1분 26초). 리뷰어가 서버 격리 사본 `/root/rev50/{deploy,docs}`
  에서 잰 값이다(CR-050 · CR50-8). 위 271 은 **그 라운드의 기록으로만** 남긴다.
  변이 3종이 **각각 죽는다**(같은 격리 사본에서 실측):
  ① `is-active` 판정 제거(항상 active 로 본다) → **실패 3** (데몬 사망·해소 미탐지 + 한 사고에 두 통이 나간다) ·
  ② 규칙 존재 검사 제거(항상 걸려 있다고 본다) → **실패 4** (규칙 소실·포트 불일치·요약 문구·해소) ·
  ③ 해소 경로 3곳 제거 → **실패 6**(세 경보가 전부 안 꺼지고 `.active` 가 남는다).
  ⚙️ 이 저장소가 **다섯 번** 놓친 것이 "clear 경로 무관측"이라(CR40·42-3·43-1·44-2·47-2),
  이번 검사는 세 해소 경로를 **행동으로** 본다(통보 및 `.active` 삭제까지).
- **`IPv6`(SR-046) 라운드(2026-08-05) 실측** — 서버 격리 사본(`/root/v6-fix` · **문서까지 트리
  구조 그대로** 복사)에서 **통과 341 · 실패 0 · 건너뜀 1 · 하네스오류 0 · rc=0**(직전 290 ·
  소요 1분 44초). 건너뜀 1은 늘 그 자리다(`oomkill` 상태손상은 cgroup·docker 가 있어야 도달).
  ⚠️ `deploy/` 만 복사하면 문서 검사가 SKIP 으로 늘어난다 — 관문을 근거로 쓰려면 트리를 지킬 것.
  변이 3종이 **각각 죽는다**(같은 격리 사본에서 실측):
  ① `_v6_scan` 호출을 지우고 **항상 `ok`** 로 본다 → **실패 10**
     (규칙 소실·순서 무력화·하위 체인 감시불능·가짜 DROP·요약 문구·해소가 전부 무너진다) ·
  ② 유닛 판정을 지우고 **항상 `enabled`** 로 본다 → **실패 12**
     (disabled·유닛 삭제·`failed`·systemd 불가에서 **거짓 해소**까지 나간다) ·
  ③ `clear_alert v6ssh_rule`·`v6ssh_unit` 두 줄 삭제 → **실패 4**(두 경보가 안 꺼지고 `.active` 가 남는다).
  ⚙️ 세 번째를 따로 두는 이유는 이 저장소가 **다섯 번** 놓친 것이 "clear 경로 무관측"이기
  때문이다(CR40·42-3·43-1·44-2·47-2). 새 경보 2종도 해소를 **행동으로** 본다(통보 + `.active` 삭제).
- **관문 자신도 오탐이 결함이다**(CR41-6): 조건 자리에 파이프라인을 쓰지 않는다.
  `set -o pipefail` 아래서 파이프라인 상태는 "안 맞았다" 말고도 0이 아닐 수 있다.
  8개 동시 실행 32회 전부 그린으로 확인했다.
